---
phase: 04-nlp-extras-quality-gate
plan: 05
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - README.md
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
autonomous: true
requirements: [QUAL-03]
must_haves:
  truths:
    - "A friend can follow the README from export to results: one-liner → export steps → install → one command → NLP download question meaning (D-18)"
    - "README presents the 3 NLP download options neutrally with sizes and no recommendation (D-19)"
    - "README documents the no-flag reality: one command, report always generated to the current working directory (D-08/D-09 reconciliation)"
    - "REQUIREMENTS.md traceability reflects ANAL-07/ANAL-09 as always-on (not [nlp]-gated) and OUT-04/OUT-05 as resolved no-flag (reconciliation notes #1 and #2)"
    - "ROADMAP Phase 4 success criteria #4/#5 no longer describe --output / --no-report flags (reconciliation note #1)"
  artifacts:
    - path: "README.md"
      provides: "quickstart-first friend documentation"
      contains: "## Quickstart"
    - path: ".planning/REQUIREMENTS.md"
      provides: "reconciled traceability"
      contains: "always-on"
    - path: ".planning/ROADMAP.md"
      provides: "flag-free Phase 4 criteria"
      not_contains: "--output"
  key_links:
    - from: "README.md"
      to: "pip install chat-analyzer-pro[nlp]"
      via: "install section + NLP options section"
      pattern: "\\[nlp\\]"
    - from: ".planning/REQUIREMENTS.md"
      to: "ANAL-07 / ANAL-09 rows"
      via: "traceability table"
      pattern: "ANAL-07"
---

<objective>
Rewrite README.md quickstart-first so a friend (the target user: no flags, no docs-reading) can go from WhatsApp/Telegram export to results in minutes (D-18), present the NLP download options neutrally (D-19), and reconcile the planning docs to the locked Phase 4 decisions: ANAL-07/ANAL-09 always-on, OUT-04/OUT-05 no-flag, report always to cwd.

Purpose: QUAL-03 requires a README "a friend can follow". The current README is a 3-line stub (PATTERNS.md no-analog row). This plan also fixes the doc drift flagged by the orchestrator's reconciliation notes — ROADMAP success criteria #4/#5 and REQUIREMENTS traceability still describe flags and [nlp]-gating that D-07/D-08 superseded.

Output: Friend-followable README + reconciled REQUIREMENTS.md/ROADMAP.md.

**Plan slice story:** **As a** friend of the developer, **I want to** follow the README from export to insights, **so that** I can try the tool in minutes without learning any flags.
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-nlp-extras-quality-gate/04-CONTEXT.md  (D-18, D-19 locked; D-08/D-09 no-flag + cwd report)
@.planning/phases/04-nlp-extras-quality-gate/04-PATTERNS.md  (no-analog row for README + REQUIREMENTS)
@README.md (current 3-line stub — read before rewriting)
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@AGENTS.md (project conventions the README must reflect: python >=3.11, lean base, [nlp] extra)

<interfaces>
D-18 required README structure (locked — implement in this order):
1. One-line "what this is"
2. Export instructions for WhatsApp and Telegram
3. Install (base + [nlp] extra)
4. The single command (no flags)
5. What the NLP download question means

D-19 (locked): the 3 NLP download options presented NEUTRALLY — all three with sizes, NO recommendation (contrast: the interactive menu may default option 2 per D-04; the README must not).

Doc reconciliation targets (from orchestrator reconciliation notes):
- ROADMAP.md Phase 4 success criteria #4 (`--output` path flag) and #5 (`--no-report`) → rewrite to no-flag wording: report always generated to the current working directory as `<chat_name>_report.html` and auto-opens
- ROADMAP.md Phase 4 Goal line → drop the "(OUT-04 --output path flag, OUT-05 --no-report semantics)" parenthetical; replace with the no-flag resolution phrasing
- REQUIREMENTS.md ANAL-07 row → remove "requires [nlp] extra" → "always-on (D-07)"
- REQUIREMENTS.md ANAL-09 row → remove "requires [nlp] extra" → "always-on (D-07b)"
- REQUIREMENTS.md OUT-04 row → mark resolved: no flag; report always saved to the current working directory (D-08/D-09)
- REQUIREMENTS.md OUT-05 row → mark resolved: no flag; report always generated (D-08)
- REQUIREMENTS.md Traceability table rows for ANAL-07/09/OUT-04/05 → reflect the above; mark them Complete-on-Phase-4 when this plan lands
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README quickstart-first (D-18)</name>
  <files>README.md</files>
  <read_first>README.md (current stub — read in full before rewriting), AGENTS.md (conventions to reflect), pyproject.toml (exact package name chat-analyzer-pro, python >=3.11, [nlp] extra)</read_first>
  <action>
    Rewrite README.md from scratch per D-18 (the current file is a 3-line stub). Sections in EXACT order:
    1. Title + one-liner: what the tool is (one command turns a WhatsApp .txt or Telegram .json export into terminal insights + a self-contained HTML report; local, no accounts).
    2. "## Quickstart" with the two export steps first:
       - WhatsApp: open the chat → ⋮ menu → More → Export chat → save the .txt
       - Telegram: Telegram Desktop → Settings → Advanced → Export Telegram data → Messages only → JSON
    3. Install: `pip install chat-analyzer-pro` (lean base) and `pip install chat-analyzer-pro[nlp]` (adds torch + transformers for emotion and summarization). State Python >= 3.11 requirement.
    4. The single command: `chat-analyzer <path-to-export>` — no flags (D-08: explicit sentence "There are no flags — one command does everything"). Explain what happens: terminal shows progress, analysis summary, report saved to the CURRENT WORKING DIRECTORY as `<chat_name>_report.html`, auto-opens in the browser (degrades to printing the absolute path).
    5. "What does the NLP download question mean?" — explain the one-time 3-option menu shown when NLP isn't installed (only on interactive terminal runs; positional runs print a single hint line instead). Do NOT include the option copy here — Task 2 owns the neutral options block; in Task 1 leave a placeholder heading with the explanation text (what the question is, that it's asked once, that answering 3 runs basic analysis).
    Keep it short enough that "a friend never reads past step 2 before trying it" (D-18).
  </action>
  <acceptance_criteria>
    - README.md contains "## Quickstart" and both export instructions ("Export chat" and "Export Telegram data")
    - README.md contains "chat-analyzer-pro[nlp]" and ">= 3.11" (or "Python 3.11")
    - README.md contains an explicit no-flags sentence and the current-working-directory report location
    - README.md does NOT contain `--output` or `--no-report` as usable flags
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path README.md -Pattern '## Quickstart','Export chat','Export Telegram data','\[nlp\]','3\.11' | Measure-Object -Line | ForEach-Object { $_.Lines -ge 5 }</automated>
  </verify>
  <done>Quickstart-first README with export steps, install, one command, and the download-question explanation</done>
</task>

<task type="auto">
  <name>Task 2: Neutral NLP options block (D-19) + planning-doc reconciliation</name>
  <files>README.md, .planning/REQUIREMENTS.md, .planning/ROADMAP.md</files>
  <read_first>README.md (the placeholder section from Task 1), .planning/REQUIREMENTS.md (ANAL-06..09 + OUT-04/05 rows + Traceability table), .planning/ROADMAP.md (Phase 4 Goal line + success criteria #4/#5 + Plans list), .planning/phases/04-nlp-extras-quality-gate/04-CONTEXT.md (D-07/D-07b/D-08/D-19 wording)</read_first>
  <action>
    Three files:

    1. README.md — replace the Task 1 placeholder with the D-19 neutral options presentation: a short list of the three options with sizes and NO recommendation wording (e.g., "1) Full torch (~3 GB) — best quality", "2) CPU-only torch + model (~0.6 GB)", "3) No download — run basic analysis"). Explicitly avoid "recommended" / "best choice" adjectives for any option (neutral per D-19 — the menu may default option 2, the README must not recommend).
    2. .planning/REQUIREMENTS.md — per the <interfaces> reconciliation targets:
       - ANAL-07 row: "requires `[nlp]` extra" → "always-on (no torch/transformers needed — D-07)"
       - ANAL-09 row: "requires `[nlp]` extra" → "always-on (networkx/matplotlib only — D-07b)"
       - OUT-04 row: replace the `--output` description with "Resolved as NO FLAG (D-08): report always saved to the current working directory"
       - OUT-05 row: replace with "Resolved as NO FLAG (D-08): report always generated; no opt-out"
       - Traceability table: mark ANAL-07, ANAL-09, OUT-04, OUT-05 rows as "Complete (Phase 4, no-flag/always-on resolution)" and update the "Coverage" note if it counts pending rows.
    3. .planning/ROADMAP.md — Phase 4:
       - Goal line: remove the parenthetical "(OUT-04 `--output` path flag, OUT-05 `--no-report` semantics)" → "(OUT-04/OUT-05 resolved as no-flag: report always generated to the current working directory)"
       - Success criterion #4: replace "--output (report written there...)" with "User's report is always saved to the current working directory as `<chat_name>_report.html` and auto-opens (no flags — D-08/D-09)"
       - Success criterion #5: replace the "--no-report" text with "The report is the deliverable and is always generated — there is no skip flag (D-08)"
       - Plans line: update `**Plans:** TBD` → `**Plans:** 5 plans` with the plan list entries (04-01 always-on health/network slice, 04-02 gated emotion/summary slice, 04-03 CLI UX menu+hint+errors, 04-04 legacy test rewire, 04-05 README + doc reconciliation)
    Do not touch Phase 1/2/3 sections or requirement rows outside the reconciliation scope.
  </action>
  <acceptance_criteria>
    - README.md contains the three options with sizes and does NOT contain "recommended" or "best choice" in the options block (grep case-insensitive)
    - REQUIREMENTS.md ANAL-07/09 rows say "always-on"; OUT-04/05 rows say "no flag"; traceability marks them Complete
    - ROADMAP.md Phase 4 contains no `--output` or `--no-report` flag descriptions in the Goal/criteria lines; Plans list shows 5 plans
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path .planning\REQUIREMENTS.md -Pattern 'always-on' | Measure-Object -Line | ForEach-Object { $_.Lines -ge 2 }</automated>
  </verify>
  <done>Neutral options block live; REQUIREMENTS.md/ROADMAP.md reconciled to D-07/D-08/D-19</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| README instructions → user terminal | commands in the README are executed by the friend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-17 | Tampering | README install instructions | mitigate | README references only the real package name (`chat-analyzer-pro`), the real extra (`[nlp]`), and the real command (`chat-analyzer`) — no invented flags or URLs; `pip install` commands are exactly what pyproject.toml declares |
| T-04-18 | Spoofing | README NLP options bias | mitigate | D-19 neutrality enforced with an explicit no-recommendation rule in the options block; the grep acceptance criterion rejects "recommended"/"best choice" |
| T-04-19 | Tampering | planning-doc drift | mitigate | REQUIREMENTS.md/ROADMAP.md edits are scoped to the reconciliation targets (ANAL-07/09, OUT-04/05, Phase 4 criteria); acceptance criteria pin the exact changed rows |
| T-04-SC | Tampering | dependency installs | accept | no packages involved; doc-only plan |
</threat_model>

<verification>
- README.md: quickstart-first order verified by reading; no-flags sentence present; options block neutral (no recommendation words)
- REQUIREMENTS.md: ANAL-07/ANAL-09 "always-on", OUT-04/OUT-05 "no flag" — grep verified
- ROADMAP.md: Phase 4 criteria #4/#5 flag-free; Plans list = 5 plans
- No code files modified by this plan
</verification>

<success_criteria>
- QUAL-03 satisfied: a friend follows README steps 1-2 (export) then step 4 (one command) and gets results
- D-19 satisfied: three options with sizes, no recommendation
- Reconciliation notes #1 and #2 closed: no flag wording anywhere in the docs; ANAL-07/09 correctly labeled always-on
</success_criteria>

<output>
Create `.planning/phases/04-nlp-extras-quality-gate/04-05-SUMMARY.md` when done
</output>
