---
phase: 04-nlp-extras-quality-gate
plan: 03
type: execute
wave: 3
depends_on: ["04-02"]
files_modified:
  - tests/test_phase4_cli.py
  - src/chat_analyzer/cli/main.py
  - src/chat_analyzer/cli/nlp_gate.py
autonomous: true
requirements: [CLI-04, QUAL-02]
must_haves:
  truths:
    - "A positional run with NLP missing prints exactly one hint line and exits 0 — never prompts (D-06)"
    - "An interactive terminal (tty) run with NLP missing shows the 3-option download menu: 1) full torch ~3GB, 2) CPU-only torch + model ~0.6GB (default), 3) no download (D-04)"
    - "The runtime install is a guarded subprocess pip (no shell=True) and announces model name + size before any download (D-05, Don't Hand-Roll row 3)"
    - "Missing-file, wrong-format, and empty-chat failures each exit 1 with a DISTINCT friendly message plus inline WhatsApp/Telegram export instructions (D-13/D-14)"
    - "Interactive no-arg runs re-prompt on bad files; positional runs exit (D-15)"
    - "No traceback ever reaches the user (D-13)"
  artifacts:
    - path: "src/chat_analyzer/cli/nlp_gate.py"
      provides: "guarded runtime installer + install_nlp(cpu_only)"
      contains: "def install_nlp"
    - path: "src/chat_analyzer/cli/main.py"
      provides: "menu dispatch + hint line + error taxonomy + export instructions"
      contains: "_EXPORT_WHATSAPP"
    - path: "tests/test_phase4_cli.py"
      provides: "subprocess proof of hint / menu / error UX"
      exports: ["test_positional_hint_line"]
  key_links:
    - from: "src/chat_analyzer/cli/main.py"
      to: "chat_analyzer.cli.nlp_gate.nlp_available"
      via: "startup silent check (D-02)"
      pattern: "nlp_available\\("
    - from: "src/chat_analyzer/cli/main.py"
      to: "chat_analyzer.cli.nlp_gate.install_nlp"
      via: "menu options 1/2 dispatch"
      pattern: "install_nlp\\("
    - from: "src/chat_analyzer/cli/main.py"
      to: "typer.Exit(code=1)"
      via: "every positional failure path"
      pattern: "Exit\\(code=1\\)"
---

<objective>
Deliver the CLI UX half of the always-integrated NLP model: the silent startup availability check, the interactive 3-option download menu (TTY-only), the single hint line for non-interactive runs, the guarded runtime `[nlp]` installer with pre-download announcement, and the CLI-04 friendly-error taxonomy with inline WhatsApp/Telegram export instructions.

Purpose: D-01..D-06 define the "always integrated" flow — the friend clones, runs, and is asked ONE question (or gets one hint line), never a flag, never a traceback. D-13/D-14 make every failure actionable. This plan is the entire user-facing contract of the NLP download experience and the error quality gate.

Output: Working `chat-analyzer` UX where NLP silently enriches when available, asks once when interactive+missing, hints when positional+missing, and fails every file-type error with distinct, instructive messages.

**Plan slice story:** **As a** user who hasn't installed NLP yet, **I want to** get a clear 3-option download question (or a single hint line) and friendly errors with export instructions, **so that** I always know exactly what to do next.
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-nlp-extras-quality-gate/04-CONTEXT.md  (D-01..D-06, D-13, D-14, D-15 locked; agent discretion: menu rendering, installer guards, error copy)
@.planning/phases/04-nlp-extras-quality-gate/04-RESEARCH.md  (Don't Hand-Roll row 3 subprocess pip, Pitfall 4 announce, Pitfall 5 gate-testability, Pattern 1 announce-inside-guarded-block)
@.planning/phases/04-nlp-extras-quality-gate/04-PATTERNS.md  (main.py insertion points: positional branch 81-99, interactive loop 101-118; friendly-error discipline)
@src/chat_analyzer/cli/main.py
@src/chat_analyzer/cli/nlp_gate.py
@tests/test_phase2_cli.py
@tests/test_phase1_smoke.py

<interfaces>
From src/chat_analyzer/cli/main.py (current — the insertion points):
- Positional branch (lines 81-99): `if chat_file is not None:` → file-not-found echo + typer.Exit(1); unsupported-suffix echo + Exit(1); `try: _analyze_path(chat_file) except ValueError as exc: typer.echo(str(exc), err=True); raise typer.Exit(code=1) from None`; `raise typer.Exit(code=0)`.
- Interactive loop (lines 101-118): `while True:` path prompt → not-file → echo + continue; bad suffix → echo + continue; `try: _analyze_path(path) except ValueError as exc: typer.echo(...); continue`; `raise typer.Exit(code=0)`.
- `_analyze_path(path)` (36-56): runs pipeline, prints `Messages: N` (CRITICAL #1 smoke token — MUST NOT be moved/removed), writes report, `show_summary`, `open_report`.
- Keep: --version eager callback (28-33), Windows utf-8 reconfigure (73-79), lazy imports in _analyze_path.

From src/chat_analyzer/cli/nlp_gate.py (created in 04-02 — this plan EXTENDS):
- Existing: `MODEL_ID = "bhadresh-savani/distilbert-base-uncased-emotion"`, `EMOTION_MODEL_SIZE_MB = 255`, `SUMMARY_MODEL_ID = "t5-small"`, `SUMMARY_MODEL_SIZE_MB = 231`, `model_cached(model_id)`, `nlp_available(model_id=MODEL_ID)` (env override CHAT_ANALYZER_FORCE_NLP already honored).
- This plan adds: `install_nlp(cpu_only: bool) -> None` — guarded subprocess runtime install (RESEARCH lines 233-243):
  `cmd = [sys.executable, "-m", "pip", "install", "torch", "transformers>=4.30,<6"]` and for cpu_only append `["--index-url", "https://download.pytorch.org/whl/cpu"]`; `subprocess.run(cmd, capture_output=True, text=True)`; on returncode != 0 raise RuntimeError (caller degrades to basic + hint, never a frozen terminal — Pitfall 4). NEVER os.system, NEVER shell=True.
- Full-torch option (~3GB) = install without --index-url; CPU-only option (~0.6GB) = with --index-url.

Menu contract (D-04): shown ONLY when (a) no-arg interactive run, (b) `sys.stdin.isatty()` is True (a piped no-arg run cannot answer a menu — it degrades to basic + hint, preserving the Phase 1/2 piped interactive tests), (c) `nlp_available()` is False. Options: 1) "Download full torch (~3GB) — best quality", 2) "Download CPU-only torch + model (~0.6GB)" (default per D-04), 3) "No download — run basic analysis". Option 2 default via rich Prompt.ask default="2".
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Failing subprocess tests for hint, menu, and error taxonomy</name>
  <files>tests/test_phase4_cli.py</files>
  <read_first>tests/test_phase2_cli.py (the _run/_cli_cmd/_copy_sample helpers, BROWSER=__none__ pattern), tests/test_phase1_smoke.py (run_cli pattern), src/chat_analyzer/cli/main.py (current error copy to assert against)</read_first>
  <behavior>
    - Test 1 (positional hint): env CHAT_ANALYZER_FORCE_NLP=0, run `chat-analyzer <copied sample>` → exit 0, stdout contains the hint line (pip install chat-analyzer-pro[nlp]) and does NOT contain a menu (no "torch (~3GB)" text), "Messages: 27" still present.
    - Test 2 (piped no-arg hint): CHAT_ANALYZER_FORCE_NLP=0, pipe the sample path → exit 0, hint line present, no menu text (piped = not a tty → menu suppressed).
    - Test 3 (missing file): positional nonexistent.txt → exit 1, "File not found" AND "Export chat" instruction text present, no Traceback.
    - Test 4 (wrong format): positional chat.pdf → exit 1, "Unsupported file type" AND export instruction present, no Traceback.
    - Test 5 (empty/unparseable): positional all-skipped fixture → exit 1, "No messages could be parsed" AND export instruction present, no Traceback.
    - Test 6 (interactive re-prompt): pipe "chat.pdf\n" (bad) then a valid path → re-prompt happens (no exit 1), valid path produces "Messages: 27" and exit 0.
    - Test 7 (menu on tty): in-process — patch sys.stdin.isatty → True, patch nlp_gate.nlp_available → False, patch main's prompt → "3"; call the app's interactive branch; assert the 3 options were rendered. (Subprocess cannot fake a tty without pty — this is an in-process unit test of the menu function.)
  </action>
  Create tests/test_phase4_cli.py (pytest style). Copy the `_run`/`_copy_sample` helper shape from test_phase2_cli.py; for tests 1-6 set `env["CHAT_ANALYZER_FORCE_NLP"] = "0"` to force the basic path deterministically (Pitfall 5 — the dev machine may cache the model after a real run; the env hook removes the flake). Use data/sample_chats/whatsapp_sample.txt and tests/fixtures/whatsapp_all_skipped.txt. For test 7, import main.py functions and use unittest.mock to patch `sys.stdin.isatty` (via the main module's reference), `chat_analyzer.cli.nlp_gate.nlp_available`, and capture the rendered menu from the Console output. Do NOT implement production code — tests must fail now (e.g., no hint line printed, no export instructions in errors). Run and confirm RED.
  </action>
  <acceptance_criteria>
    - tests/test_phase4_cli.py exists with tests 1-7
    - `python -m pytest tests/test_phase4_cli.py -x` fails for at least the hint/instruction assertions (RED)
    - No production source file modified
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_phase4_cli.py -x; expect FAIL (RED)</automated>
  </verify>
  <done>Failing UX tests prove the menu/hint/error contract is not yet delivered</done>
</task>

<task type="auto">
  <name>Task 2: nlp_gate installer + main.py menu dispatch + hint line</name>
  <files>src/chat_analyzer/cli/nlp_gate.py, src/chat_analyzer/cli/main.py</files>
  <read_first>src/chat_analyzer/cli/nlp_gate.py (current availability probe to extend), src/chat_analyzer/cli/main.py (full file — positional branch, interactive loop, _analyze_path, smoke token), tests/test_phase1_smoke.py (test_prompt_happy_path + test_invalid_path_reprompts must stay green — piped runs must NOT hit the menu)</read_first>
  <action>
    Per D-04/D-05/D-06. Two files:

    1. nlp_gate.py — add `install_nlp(cpu_only: bool) -> None` per the <interfaces> contract: subprocess.run([sys.executable, "-m", "pip", "install", "torch", "transformers>=4.30,<6"] + (["--index-url", "https://download.pytorch.org/whl/cpu"] if cpu_only else []), capture_output=True, text=True); if returncode != 0 raise RuntimeError("Model install failed — run basic analysis, or install: pip install chat-analyzer-pro[nlp]") (RESEARCH line 241 copy anchor); no shell=True. Add module docstring noting it is a runtime re-install of already-declared [nlp] extras (no new packages).
    2. main.py —
       (a) Lazy-import nlp_gate at the top of the command body (keep --help instant; import inside `main()` before use, not at module import).
       (b) Startup silent check (D-02): in `main()`, compute `nlp_on = nlp_gate.nlp_available(nlp_gate.MODEL_ID)` ONCE; do not prompt here.
       (c) Interactive branch (no-arg + valid path + inside the try): if `not nlp_on` AND `sys.stdin.isatty()`: show the 3-option menu via rich (Console() prompt or typer.prompt with a numbered menu; default option 2; rendering at agent discretion). Option "1"/"2" → `console.print` the announcement (e.g., `[INFO] Installing NLP extras: torch + transformers (~0.6 GB CPU / ~3 GB full), then model bhadresh-savani/distilbert-base-uncased-emotion (~255 MB)`) → `nlp_gate.install_nlp(cpu_only=opt=="2")` → on RuntimeError: echo the friendly failure + continue hint and fall through to basic analysis (never a frozen terminal — Pitfall 4). Option "3" → basic analysis. Implement the menu as a module-level function `_nlp_menu(console) -> str` returning "1"/"2"/"3" so test 7 can unit-test it directly.
       (d) Hint line (D-06): after `_analyze_path` returns in BOTH the positional branch and the piped/no-menu interactive path, when `not nlp_on`: `console.print("[INFO] Tip: richer insights need the NLP extra — pip install chat-analyzer-pro[nlp]")`. Print AFTER show_summary (report path line), never before "Messages: N". ASCII only (no emoji — render.py convention).
       (e) Keep the smoke token "Messages: N" exactly where it is (main.py line 50, CRITICAL #1), the --version callback, utf-8 reconfigure, and both branch structures.
  </action>
  <acceptance_criteria>
    - nlp_gate.py contains `def install_nlp` with subprocess.run([sys.executable, "-m", "pip", ...]) and no shell=True
    - main.py contains `_nlp_menu`, the `sys.stdin.isatty()` guard, the `install_nlp(cpu_only=...)` dispatch, and the hint line string `pip install chat-analyzer-pro[nlp]`
    - `python -m pytest tests/test_phase4_cli.py -x` passes tests 1, 2, 6, 7 (GREEN on hint/menu; error tests 3-5 still RED — they land in Task 3)
    - `python -m pytest tests/test_phase1_smoke.py tests/test_phase2_cli.py -x` passes (piped interactive tests unaffected — menu is tty-gated; hint line does not trip test_no_console_pollution which checks 🚀/Initializing Sentiment only)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_phase4_cli.py tests/test_phase1_smoke.py tests/test_phase2_cli.py -x</automated>
  </verify>
  <done>Menu (tty), hint (non-tty/positional), and guarded installer are live; Phase 1/2 CLI tests unaffected</done>
</task>

<task type="auto">
  <name>Task 3: Friendly-error taxonomy with inline export instructions (CLI-04)</name>
  <files>src/chat_analyzer/cli/main.py</files>
  <read_first>src/chat_analyzer/cli/main.py (current error sites: lines 82-90 file-not-found + unsupported-suffix in both branches; the ValueError handler at 93-98 and 114-117), src/chat_analyzer/cli/pipeline.py (the exact ValueError messages raised: "Unsupported file type: ..." at 88-91 and "No messages could be parsed from this file" at 107)</read_first>
  <action>
    Per D-13/D-14/D-15. One file. Add the export-instruction constants and a classifier, then wire them into every error path:

    1. Module-level constants (copy at agent discretion, keep them short and exact-step):
       - `_EXPORT_WHATSAPP = "To export a chat: open the chat in WhatsApp, tap the ⋮ menu, More -> Export chat, and save the .txt file."`
       - `_EXPORT_TELEGRAM = "To export a chat: in Telegram Desktop open Settings -> Advanced -> Export Telegram data, select only Messages, choose JSON format, and save the folder export."`
    2. `_friendly_error(chat_file: Path, exc: Exception) -> str` — classifies the failure by substring and composes the distinct message + instructions:
       - File-not-found case (handled in main() before try): `f"File not found: {chat_file}. The file must exist before running. {_EXPORT_WHATSAPP} {_EXPORT_TELEGRAM}"`
       - "Unsupported file type" in str(exc): `f"Unsupported file type: expected a WhatsApp .txt or Telegram .json export. {_EXPORT_WHATSAPP} {_EXPORT_TELEGRAM}"`
       - "No messages could be parsed" in str(exc): `f"No messages could be parsed from this file — the export may be empty or a system-only export. {_EXPORT_WHATSAPP} {_EXPORT_TELEGRAM}"`
       - anything else (defensive): `f"Could not process this file. {str(exc)} {_EXPORT_WHATSAPP} {_EXPORT_TELEGRAM}"`
    3. Wire into the POSITIONAL branch: replace the bare `typer.echo(f"File not found: {chat_file}", err=True)` with `typer.echo(_friendly_error(chat_file, FileNotFoundError()), err=True)` (message classification by the known state), replace the unsupported-suffix echo with `typer.echo(_friendly_error(chat_file, ValueError("Unsupported file type")), err=True)`, and the ValueError handler with `typer.echo(_friendly_error(chat_file, exc), err=True)`. Keep `raise typer.Exit(code=1) from None` everywhere (exit code 1, no traceback — D-13).
    4. Wire into the INTERACTIVE branch: same three echo sites but `continue` instead of exit (D-15 re-prompt loop preserved).
    5. Do NOT change the smoke token, the report flow, or open_report.
  </action>
  <acceptance_criteria>
    - main.py contains `_EXPORT_WHATSAPP` and `_EXPORT_TELEGRAM` constants and a `_friendly_error` function
    - Every positional error path ends in `typer.Exit(code=1) from None`
    - Interactive loop keeps `continue` (re-prompt) after each friendly echo
    - `python -m pytest tests/test_phase4_cli.py -x` passes all 7 tests (GREEN)
    - `python -m pytest tests/test_phase2_cli.py tests/test_phase1_smoke.py -x` passes — including test_unsupported_and_error_paths (asserts "expected a WhatsApp .txt or Telegram .json" substring — still present inside the composed message) and test_unprocessable_input_exits_one ("File not found" substring still present)
    - `python -m ruff check src/chat_analyzer tests` passes (0 errors)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_phase4_cli.py tests/test_phase2_cli.py tests/test_phase1_smoke.py -x && python -m ruff check src/chat_analyzer tests</automated>
  </verify>
  <done>Every file-type failure exits 1 (or re-prompts) with a distinct message and inline export steps; zero tracebacks</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| terminal stdin → menu | user choice crosses into a runtime pip install |
| subprocess pip output → terminal | install output is captured, never echoed raw |
| error path → stderr | failure copy composed from constants + exception text |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-10 | Tampering | runtime pip install (nlp_gate.install_nlp) | mitigate | `subprocess.run([sys.executable, "-m", "pip", "install", ...], capture_output=True)` — no shell=True, no os.system, fixed package names from the already-audited [nlp] extra (Don't Hand-Roll row 3); failure raises RuntimeError → caller degrades to basic + hint |
| T-04-11 | Information Disclosure | raw traceback to user | mitigate | every failure path ends in `_friendly_error(...)` composed copy + `typer.Exit(code=1) from None`; exception text is wrapped, never dumped (D-13) |
| T-04-12 | DoS (UX) | frozen first-run download | mitigate | menu options announce model name + size BEFORE install_nlp/download; offline/no-pip → RuntimeError → basic analysis + hint, never a frozen terminal (Pitfall 4) |
| T-04-13 | Spoofing | menu default coercion | mitigate | option 2 (CPU ~0.6GB) is the default per D-04; option 3 (no download) is always available; README presents all three neutrally (D-19, plan 04-05) |
| T-04-SC | Tampering | dependency installs | accept | only torch/transformers (already in pyproject `[nlp]` extra, audited T-01-SC in Phase 1) are installed at runtime; no new package names enter the dependency graph |
</threat_model>

<verification>
- `python -m pytest tests/test_phase4_cli.py tests/test_phase2_cli.py tests/test_phase1_smoke.py -x` — all green
- `python -m ruff check src/chat_analyzer tests` — 0 errors
- Manual tty check (dev machine, NLP not cached): run `chat-analyzer` interactively → 3-option menu appears once; positional run → hint line + exit 0; nonexistent file → distinct message + export steps + exit 1
- Smoke contract: `Messages: N` still printed exactly once in both branches
</verification>

<success_criteria>
- D-04 menu live for interactive tty users; D-06 single hint line for positional/piped users
- Guarded installer announces name + size before download and degrades gracefully offline
- CLI-04 complete: four failure classes each with a distinct friendly message + inline WhatsApp/Telegram export instructions + exit code 1 (positional) / re-prompt (interactive)
- Zero tracebacks in all covered failure paths
</success_criteria>

<output>
Create `.planning/phases/04-nlp-extras-quality-gate/04-03-SUMMARY.md` when done
</output>
