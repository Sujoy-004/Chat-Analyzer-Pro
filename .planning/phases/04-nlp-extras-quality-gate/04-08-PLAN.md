---
phase: 04-nlp-extras-quality-gate
plan: 08
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chat_analyzer/cli/nlp_gate.py
  - src/chat_analyzer/cli/result_cache.py        # NEW
  - src/chat_analyzer/cli/pipeline.py
  - src/chat_analyzer/cli/zip_input.py
  - scripts/benchmark.py
  - tests/test_result_cache.py                    # NEW
  - README.md
  - .planning/phases/04-nlp-extras-quality-gate/04-08-SUMMARY.md
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "User who sets CHAT_ANALYZER_RESULT_CACHE and re-runs the same file sees `[INFO] Loaded analysis from cache` and a repeat run that completes in seconds without re-running the compute/NLP stages"
    - "First run with the env var set behaves exactly like today: all stages run, the report is written fresh to the current working directory and auto-opened (D-09/D-10 unchanged)"
    - "Editing the file bytes, changing tier (nlp_on), sample-cap choice, or worker count produces a miss (full re-analysis) — a cached result is never served for a different input/config"
    - "With the env var unset (default OFF), nothing changes: no cache directory is created anywhere and behavior is byte-identical to today"
    - "Cache files live OUTSIDE the repo tree (privacy lock): %LOCALAPPDATA%\\chat-analyzer\\cache on Windows, ~/.cache/chat-analyzer elsewhere — never under the working tree, nothing new in git status"
    - "Corrupt, schema-mismatched, or older-than-30-day cache entries degrade to a miss — never a crash; corrupt files self-heal (deleted), stale entries pruned on store"
    - "The cached payload never contains raw chat text and is loaded with json.load only — never pickle, never eval (Tampering)"
  artifacts:
    - path: "src/chat_analyzer/cli/nlp_gate.py"
      provides: "env parsers result_cache_enabled()/result_cache_dir() + RESULT_CACHE_SCHEMA constant"
      contains: "RESULT_CACHE_SCHEMA"
    - path: "src/chat_analyzer/cli/result_cache.py"
      provides: "sha256_file, cache_key, _sanitize, load, store, _prune (NEW module, stdlib only)"
      contains: "def cache_key"
    - path: "src/chat_analyzer/cli/pipeline.py"
      provides: "cache lookup/store hook inside run_pipeline after sample resolution"
      contains: "Loaded analysis from cache"
    - path: "src/chat_analyzer/cli/zip_input.py"
      provides: "chosen transcript member names surfaced to the pipeline (rides in the key)"
      contains: "chosen_names"
    - path: "scripts/benchmark.py"
      provides: "CHAT_ANALYZER_RESULT_CACHE=0 in the worker env so first-run timings stay unpolluted"
      contains: "CHAT_ANALYZER_RESULT_CACHE"
    - path: "tests/test_result_cache.py"
      provides: "15 fast mocked cache tests (hit/miss/key-change/corruption/TTL/sanitizer)"
      contains: "def test_hit_skips_nlp_stages"
    - path: "README.md"
      provides: "env-var documentation + cache-deletion privacy note"
      contains: "CHAT_ANALYZER_RESULT_CACHE"
  key_links:
    - from: "src/chat_analyzer/cli/pipeline.py"
      to: "result_cache.cache_key"
      via: "lookup after the sample-cap decision, before 'Computing insights'"
      pattern: "cache_key"
    - from: "src/chat_analyzer/cli/pipeline.py"
      to: "result_cache.load / result_cache.store"
      via: "hit -> load + early return; miss -> store after adapt()"
      pattern: "result_cache\\.(load|store)"
    - from: "src/chat_analyzer/cli/result_cache.py"
      to: "nlp_gate.RESULT_CACHE_SCHEMA / result_cache_dir"
      via: "schema constant + resolved cache dir imported from nlp_gate"
      pattern: "RESULT_CACHE_SCHEMA"
    - from: "src/chat_analyzer/cli/zip_input.py"
      to: "pipeline chosen_names -> key signature"
      via: "4-tuple return; sorted member names fold into cache_key"
      pattern: "chosen"
    - from: "main.py _analyze_path"
      to: "results['report_path'] = str(write_report(results, path).resolve())"
      via: "cache load forces report_path=''; main.py always fills it fresh after write_report (D-09)"
      pattern: "report_path"
---

<objective>
Land the DEFERRED.md open follow-up: an **opt-in result cache keyed by sha256 of the input file bytes + a config signature**, so a repeat run of the same chat file completes in seconds instead of minutes/hours. The cache is a new stdlib-only module (`result_cache.py`) + a whitelist env parser in `nlp_gate.py`, hooked inside `run_pipeline` after parse and after the Option C sample-cap decision, before the ~90-110s "Computing insights" stage. Default OFF (opt-in, matching the conservative tty-default-NO UX); payload = the full AnalysisResults contract + charts + charts_json (recursive numpy sanitizer); cache dir OUTSIDE the repo (`%LOCALAPPDATA%\chat-analyzer\cache` / `~/.cache/chat-analyzer`); 30-day TTL prune-on-store; corruption → miss, never crash; `json.load` only, never pickle.

Purpose: this is the DEFERRED.md charter ("Result cache keyed by file hash (repeat run = seconds) — still an OPEN follow-up"). It makes large-chat iteration practical (424k-message runs drop from ~10-33 min to seconds on re-run) and closes the 04-06 decision record ("a file-hash cache (rejected by plan, could revisit as opt-in)" — landed as opt-in exactly as recorded). QUAL-02 is addressed by a real-module test suite driving the REAL `run_pipeline` with mocked model callables (D-17: fast, offline, no real-model inference).

Output: `result_cache.py` + `nlp_gate.py` parsers + `pipeline.py` hook + `zip_input.py` chosen-list surface + `benchmark.py` hardening + `tests/test_result_cache.py` (15 tests) + README env docs + 04-08 SUMMARY. Zero new packages (stdlib hashlib/json/os/tempfile/pathlib only — 04-07 precedent). No CLI flag surface (D-08).

**Plan slice story:** **As a** user re-analyzing the same chat export, **I want to** set one env var and have repeat runs complete in seconds with identical output, **so that** I can iterate on a large chat without waiting minutes to hours — while a clone with the var unset sees zero behavior change and zero personal data in the repo.

Authoritative design source: `.planning/phases/04-nlp-extras-quality-gate/04-08-RESEARCH.md` (Q1 key composition, Q2 payload/sanitizer, Q3 location, Q4 env contract, Q5 correctness, Q6 hook placement, Q7 test design, Q8 scope). Follow it; deviations only where this plan pins a choice the research left open.
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-nlp-extras-quality-gate/04-08-RESEARCH.md  (authoritative — read in full first)
@.planning/phases/04-nlp-extras-quality-gate/04-06-SUMMARY.md  (option-c sample-cap resolution + `sampled` contract + commit style)
@.planning/phases/04-nlp-extras-quality-gate/04-07-RESEARCH.md (emotion_worker_count contract — goes in the key)
@DEFERRED.md  (charter: "Result cache keyed by file hash" follow-up; "No real chats... anywhere in the repo" privacy lock)
@README.md  (env-var list at lines ~99-102 + Privacy section at ~133 — the 04-07 CHAT_ANALYZER_EMOTION_WORKERS doc line is UNCOMMITTED; do not clobber it)
@.planning/STATE.md  (04-06/04-07 decision records)

Working-tree note: HEAD = `07a7b57`; README.md has an uncommitted 1-line doc change (EMOTION_WORKERS, from 04-07) and 04-07/04-08-RESEARCH.md are untracked. Task 6 must preserve the uncommitted README line. 04-08-RESEARCH.md stays uncommitted per the same instruction that left 04-07-RESEARCH.md untracked.

<interfaces>
New contracts this plan creates (executor builds against these):

From src/chat_analyzer/cli/nlp_gate.py (Task 1 — mirrors emotion_sample_cap style, never raises):
```python
_RESULT_CACHE_ENV = "CHAT_ANALYZER_RESULT_CACHE"
RESULT_CACHE_SCHEMA = 1          # bump on any AnalysisResults contract / key-composition change

def result_cache_enabled() -> bool:      # whitelist: absent/empty/off-words -> False; else True
def result_cache_dir() -> Path | None:   # None = disabled; else default OS dir or explicit path
```

From NEW src/chat_analyzer/cli/result_cache.py (Task 2, stdlib only):
```python
RESULT_CACHE_TTL_DAYS = 30
def sha256_file(path: Path) -> str                       # chunked read (1 MiB blocks)
def cache_key(path, *, nlp_on, sample_cap, emotion_workers, chosen_transcripts) -> str  # hex digest
def load(cache_dir: Path, key: str) -> dict | None       # miss on any failure; self-heal corrupt file
def store(cache_dir: Path, key: str, results: dict) -> None  # sanitize + envelope + atomic write + prune; never raises
def _sanitize(obj)                                        # recursive numpy -> .item(), NaN/Inf -> None
def _prune(cache_dir: Path, max_age_days: int = RESULT_CACHE_TTL_DAYS) -> None  # best-effort
```

From src/chat_analyzer/cli/zip_input.py (Task 3 — single caller is pipeline.py:175-177):
```python
def parse_zip_with_report(zip_path, console) -> tuple[list[dict], dict, str, list[str]]
    # 4th element = sorted chosen transcript member names (all for non-tty; user pick for tty)
```

Existing contracts the executor needs (no exploration required):
- `AnalysisResults` TypedDict (contracts.py:25-53) — `report_path` is filled by main.py after `write_report` (contracts.py:52, main.py:199). Cache load must force `report_path=""`.
- `nlp_gate.emotion_sample_cap() -> int | None` (nlp_gate.py:82-109) and `nlp_gate.emotion_worker_count() -> int` (nlp_gate.py:112-139) — both already committed.
- `run_pipeline(path: Path, console, nlp_enabled: bool | None = None) -> AnalysisResults` (pipeline.py:123); Option C sample block currently at pipeline.py:301-317 (inside `if nlp_on:`) — this plan MOVES it up; `adapt(...)` call at pipeline.py:449-466; `finally: progress.stop()` at 467-469 already handles the early-return-on-hit path.
- Stage labels are a locked contract: "Parsing chat" / "Computing insights" / "Analyzing emotions" / "Generating narrative" (pipeline.py:67-81) — `test_stage_narration_and_order` asserts the first two verbatim; the relocation must not change them.
- Test mocking pattern: `tests/test_emotion_sampling.py` `_mocked_models()` (345-365), `_classifier` (96-101), `_non_tty_console()` (381-385), tty `console.input` patch (408-430), transformers-None skip guard (53-56).
- `scripts/benchmark.py` `_spawn_and_run` env block (223-227).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Env parsers + schema constant in nlp_gate.py</name>
  <files>src/chat_analyzer/cli/nlp_gate.py</files>
  <read_first>src/chat_analyzer/cli/nlp_gate.py (emotion_sample_cap at 82-109 and emotion_worker_count at 112-139 — mirror their never-raises whitelist style exactly)</read_first>
  <action>
    Add three things to nlp_gate.py, following the existing `_EMOTION_SAMPLE_ENV`/`emotion_sample_cap()` pattern (docstring + whitelist semantics + never raises):

    1. `_RESULT_CACHE_ENV = "CHAT_ANALYZER_RESULT_CACHE"` (private, next to the other `_*_ENV` constants) and `RESULT_CACHE_SCHEMA = 1` (public — the cache payload/key schema version; document "bump when the AnalysisResults contract shape or key composition changes").
    2. `result_cache_enabled() -> bool` — whitelist parse, case-insensitive, never raises:
       - absent/empty -> False
       - off-words `"0"`, `"off"`, `"false"`, `"no"` -> False
       - anything else (`"1"`/`"on"`/`"true"`/`"yes"` OR a path) -> True
    3. `result_cache_dir() -> Path | None` — None when disabled (per `result_cache_enabled()`); otherwise:
       - keyword on-value (`1`/`on`/`true`/`yes`) -> the OS default dir:
         - Windows (os.name == "nt"): `Path(os.environ["LOCALAPPDATA"]) / "chat-analyzer" / "cache"`, falling back to `Path.home() / ".cache" / "chat-analyzer"` when LOCALAPPDATA is unset or empty
         - elsewhere: `Path.home() / ".cache" / "chat-analyzer"` (matches the existing `~/.cache/huggingface` convention, nlp_gate.py:78)
       - any other non-empty value -> that value as the explicit dir `Path(value)` (garbage is indistinguishable from a path — documented; never raises)
       - SIDE-EFFECT-FREE: no mkdir here — dir creation belongs to `result_cache.store()` (Task 2). The default dir is ALWAYS outside the repo tree by construction (user-profile path).
    Import `Path` from pathlib at module top (it is not currently imported). Keep ruff-clean (the module is in the clean set since af89446).

    Commit: `feat(nlp): result cache env parsers + schema constant (04-08)`
  </action>
  <acceptance_criteria>
    - nlp_gate.py defines `_RESULT_CACHE_ENV`, `RESULT_CACHE_SCHEMA`, `result_cache_enabled()`, `result_cache_dir()`
    - No raise paths; off-words and absent/empty disable; on-words resolve to the OS default dir (LOCALAPPDATA on nt, ~/.cache/chat-analyzer elsewhere); arbitrary values resolve to that path
    - Default dir never under the repo tree; parser performs no filesystem side effects
  </acceptance_criteria>
  <verify>
    <automated>python -m ruff check src/chat_analyzer/cli/nlp_gate.py && python -c "import os; from chat_analyzer.cli import nlp_gate as g; os.environ.pop('CHAT_ANALYZER_RESULT_CACHE', None); assert g.result_cache_dir() is None; os.environ['CHAT_ANALYZER_RESULT_CACHE']='0'; assert g.result_cache_dir() is None; os.environ['CHAT_ANALYZER_RESULT_CACHE']='1'; d=g.result_cache_dir(); assert d is not None and 'chat-analyzer' in str(d); os.environ['CHAT_ANALYZER_RESULT_CACHE']='garbage'; assert str(g.result_cache_dir())=='garbage'; print('parser ok')"</automated>
    (The full behavioral gate is Task 5's `test_env_parser` / `test_cache_disabled_by_default`; the probe above is the immediate automated check.)
  </verify>
  <done>Parser contract live and side-effect-free; ruff clean on nlp_gate.py</done>
</task>

<task type="auto">
  <name>Task 2: NEW result_cache.py — key builder, sanitizer, load/store/prune</name>
  <files>src/chat_analyzer/cli/result_cache.py</files>
  <read_first>src/chat_analyzer/cli/chart_json.py lines ~542-550 (`_float_or_none` precedent for the sanitizer), src/chat_analyzer/cli/contracts.py (AnalysisResults shape — the payload), 04-08-RESEARCH.md Q1/Q2/Q5 (key composition, sanitizer necessity, corruption/TTL/atomic-write contracts)</read_first>
  <action>
    Create `src/chat_analyzer/cli/result_cache.py` (stdlib only: hashlib, json, math, os, tempfile, time, datetime, importlib.metadata, pathlib). Module docstring stating the privacy/security contract: cache lives outside the repo, payload = AnalysisResults contract only, json.load only (never pickle/eval).

    Functions (per the <interfaces> block):
    1. `sha256_file(path) -> str` — chunked read (1 MiB blocks via `path.open("rb")`), hexdigest. 20 MB files hash in ~0.1-0.3 s. Never touches mtime/size — a `touch` must still hit (research Q1.1).
    2. `_app_version() -> str` — `importlib.metadata.version("chat-analyzer-pro")` wrapped in try/except PackageNotFoundError -> "dev" (the same call main.py:85 uses; the guard keeps editable installs safe).
    3. `cache_key(path, *, nlp_on, sample_cap, emotion_workers, chosen_transcripts) -> str` — composition, deterministic and sorted:
       - `file_sha256 = sha256_file(path)` (zip inputs hash the WHOLE zip bytes — media members included, research Q1.2)
       - `signature = {"schema": nlp_gate.RESULT_CACHE_SCHEMA, "app_version": _app_version(), "nlp_on": bool(nlp_on), "sample_cap": sample_cap (None = exact), "emotion_workers": int(emotion_workers), "chosen_transcripts": sorted(chosen_transcripts or [])}`
       - `key = sha256(json.dumps({"file_sha256": file_sha256, "signature": signature}, sort_keys=True).encode()).hexdigest()`
       - Returns the hex string only — filename = `<key>.json`. Hex-only names mean no user input ever enters a path (traversal impossible).
    4. `_sanitize(obj)` — recursive walker (dict/list/tuple; everything else passes through):
       - numpy scalar types -> `.item()`: `numpy.integer` -> int, `numpy.floating` -> float, `numpy.bool_` -> bool
       - `numpy.ndarray` -> `obj.tolist()` then recurse
       - non-finite floats (NaN/±Inf via `math.isnan`/`math.isinf`) -> None (precedent: chart_json `_float_or_none`; verified leaks: stats.peak_hour, stats.avg_response_time, emotion distribution counts, sentiment means — research Q2.3)
       - Keep numpy import lazy/guarded (module must import without numpy — wrap the isinstance checks so a non-numpy env never crashes the walk).
    5. `load(cache_dir, key) -> dict | None`:
       - `path = cache_dir / f"{key}.json"`; `json.loads(path.read_text(encoding="utf-8"))`
       - except (json.JSONDecodeError, OSError, TypeError, ValueError) -> `path.unlink(missing_ok=True)` (self-heal) and return None — NEVER raises (research Q5.3)
       - not a dict, or `data.get("schema") != RESULT_CACHE_SCHEMA`, or `results = data.get("results")` not a dict -> return None (miss; schema-mismatch files are NOT deleted — a newer-format file stays for a future version)
       - on success: `results["report_path"] = ""` (never serve a stale cwd path — research Q2.4, contracts.py:52) and return `results`
    6. `store(cache_dir, key, results) -> None`:
       - `cache_dir.mkdir(parents=True, exist_ok=True)`; `_prune(cache_dir)` first (TTL on store)
       - envelope `{"schema": RESULT_CACHE_SCHEMA, "app_version": _app_version(), "created_at": <UTC ISO-8601>, "results": _sanitize(results)}`
       - ATOMIC write: `tempfile.NamedTemporaryFile("w", encoding="utf-8", dir=cache_dir, delete=False)` -> `json.dump` -> `os.replace(tmp, cache_dir / f"{key}.json")` (atomic on the same volume, Windows-safe; last-write-wins for concurrent same-file runs — research Q5.6)
       - the whole body is best-effort: wrap in try/except Exception with `logger.exception(...)` (BLE001-safe — logging call first, matches the emotion.py pattern) — store NEVER raises into the pipeline
    7. `_prune(cache_dir, max_age_days=RESULT_CACHE_TTL_DAYS)` — `os.scandir(cache_dir)`, delete `.json` entries whose `mtime` is older than `max_age_days`, best-effort try/except OSError per entry; never raises (research Q5.5). `RESULT_CACHE_TTL_DAYS = 30` module constant.

    Module-level constants: `RESULT_CACHE_TTL_DAYS = 30`. Import `RESULT_CACHE_SCHEMA` and `result_cache_dir` from `chat_analyzer.cli.nlp_gate` (do NOT re-declare the env contract here — one parsing contract, 04-06 pattern).

    Commit: `feat(nlp): file-hash result cache store (sha256 key, sanitizer, TTL) (04-08)`
  </action>
  <acceptance_criteria>
    - Module imports cleanly without numpy present (lazy/guarded numpy usage)
    - `json.dumps(store_payload)` never raises TypeError on a payload containing the verified numpy leaks (round-trip proven in Task 5 test l)
    - Corrupt/schema-mismatch loads return None without raising; corrupt file deleted; store is atomic (tmp + os.replace) and never raises
    - Key filename is hex-only; key changes when file bytes, nlp_on, sample_cap, emotion_workers, or chosen_transcripts change
  </acceptance_criteria>
  <verify>
    <automated>python -m ruff check src/chat_analyzer/cli/result_cache.py && python -c "import tempfile,os; from pathlib import Path; from chat_analyzer.cli import result_cache as rc; d=Path(tempfile.mkdtemp()); k='a'*64; rc.store(d,k,{'stats':{'peak_hour':1},'report_path':'C:\\\\old\\\\x.html'}); r=rc.load(d,k); assert r is not None and r['report_path']=='' and r['stats']['peak_hour']==1; rc.store(d,'b'*64,{'x':1}); rc.store(d,'c'*64,{'x':2}); import os as _o; print('store/load ok')"</automated>
    (Full behavioral gate: Task 5's sanitizer/corruption/TTL/key tests.)
  </verify>
  <done>result_cache.py committed, ruff clean, store/load round-trip verified, report_path blanked on load</done>
</task>

<task type="auto">
  <name>Task 3: Hook cache lookup/store into run_pipeline (sample decision moved up)</name>
  <files>src/chat_analyzer/cli/pipeline.py, src/chat_analyzer/cli/zip_input.py</files>
  <read_first>src/chat_analyzer/cli/pipeline.py (run_pipeline 123-469: parse at 158-182, df build at 196-198, "Computing insights" at 200, Option C sample block at 301-317 inside the emotion stage, adapt() at 449-466, finally at 467-469), src/chat_analyzer/cli/zip_input.py (parse_zip_with_report 149-208), 04-08-RESEARCH.md Q6 (hook placement — three verified ordering constraints)</read_first>
  <action>
    Two files:

    1. `zip_input.py` — extend `parse_zip_with_report` to return a 4-tuple `(rows, counts, source, chosen_names)`:
       - `chosen_names` = sorted member names of the chosen transcripts (the `chosen` list from `_select_transcripts`, sorted by member name) — the same list the merge loop iterates
       - Update the docstring and the return statement. `count_zip_media_members` and `_select_transcripts` are unchanged.
       - Safe: pipeline.py:177 is the ONLY caller (verified by grep) — no other consumer breaks.

    2. `pipeline.py` — restructure `run_pipeline` with NO observable behavior change on a miss (same stage labels, same prompt text, same order):
       - Capture `chosen_names` at parse: txt/json paths -> `[]`; zip path -> the new 4th element (`rows, counts, source, chosen_names = parse_zip_with_report(path, console)`).
       - After `df = messages_to_dataframe(rows)` + the empty-df guard (line 196-198), BEFORE the `with stage(..., "Computing insights")` block:
         a. MOVE the Option C sample-decision block up from its current spot (lines 301-317) — identical logic and prompt text, same `if nlp_on:` guard, `sample_cap` resolved once here. When `nlp_on` is False, `sample_cap = None` and no prompt runs (exact path unchanged). DELETE the old block from inside the emotion stage.
         b. Cache lookup: `cache_dir = nlp_gate.result_cache_dir()`; when not None:
            - `key = result_cache.cache_key(path, nlp_on=nlp_on, sample_cap=sample_cap, emotion_workers=nlp_gate.emotion_worker_count(), chosen_transcripts=chosen_names)` — the resolved effective cap (tty y/N answer collapsed in) is part of the key, so `y` and `N` on the same file produce two distinct entries (research Q1.4/A1)
            - `results = result_cache.load(cache_dir, key)`; on hit: `console.print("[INFO] Loaded analysis from cache")` (pipeline owns narration — render.py:6-8 single-source rule) and `return results` — `report_path` is already "" (Task 2), the `finally: progress.stop()` handles the early return, and main.py still writes the report fresh after `write_report` (D-09 unchanged). The tier menu in main.py is untouched — it stays the consent point and `nlp_on` exists by then.
         c. On miss: existing stages run unchanged with the pre-resolved `sample_cap` threaded into `analyze_emotions(df, sample_cap=sample_cap, workers=...)` (the variable name already matches — line 346).
       - After `results = adapt(...)` (line 451-466) and BEFORE `return results`: `if cache_dir is not None: result_cache.store(cache_dir, key, results)` (best-effort, never raises).
       - Tip hint (research Q4.2 recommendation, D-06 hint pattern): when `nlp_on` is True AND the cache is disabled (`cache_dir is None`), print `[INFO] Tip: set CHAT_ANALYZER_RESULT_CACHE=1 to make repeat runs of this file take seconds.` once before returning — only on the NLP path (basic runs are fast; a hit path never prints it because hits return early after the Loaded label).
       - Import `result_cache` lazily inside run_pipeline (mirroring the existing lazy `nlp_gate`/analysis imports at pipeline.py:134/202) so module import stays light.
       - Stage labels and order MUST stay exactly "Parsing chat"/"Computing insights"/"Analyzing emotions"/"Generating narrative" — `test_stage_narration_and_order` and the 04-06 sampling tests must stay green.

    Commit: `feat(nlp): cache lookup inside run_pipeline after sample resolution (04-08)`
  </action>
  <acceptance_criteria>
    - On a miss, output is byte-identical in behavior to today (same stage order/narration/prompt; `test_stage_narration_and_order` + 04-06 sampling tests green)
    - On a hit: `[INFO] Loaded analysis from cache` printed, no "Computing insights"/"Analyzing emotions"/"Generating narrative" stage bodies run, report_path stays "", run returns the cached payload
    - Zip parse returns chosen_names; non-zip paths pass []
    - store() called on miss only when the cache is enabled; never raises into the pipeline
  </acceptance_criteria>
  <verify>
    <automated>python -m ruff check src/chat_analyzer/cli/pipeline.py src/chat_analyzer/cli/zip_input.py && python -m pytest tests/test_phase4_nlp.py tests/test_emotion_sampling.py -q</automated>
    (The hit/miss behavior gate is Task 5's suite — tests c/d/e; the commands above prove the relocation broke nothing: stage narration + Option C sampling stay green.)
  </verify>
  <done>Hook live: sample decision resolves before the lookup, hit returns early with the Loaded label, miss stores best-effort; existing pipeline/sampling suites green</done>
</task>

<task type="auto">
  <name>Task 4: benchmark.py forces the cache off for first-run timings</name>
  <files>scripts/benchmark.py</files>
  <read_first>scripts/benchmark.py `_spawn_and_run` env block (223-227 — CHAT_ANALYZER_TIER/CHAT_ANALYZER_NO_OPEN already there)</read_first>
  <action>
    Add `env["CHAT_ANALYZER_RESULT_CACHE"] = "0"` to the `_spawn_and_run` env block in scripts/benchmark.py (after the existing `CHAT_ANALYZER_NO_OPEN` line). Rationale (research Q6.5, Pitfall 7): a user with the env var set globally would otherwise have the benchmark measure cache-hit seconds and the README timing table becomes fake. One line, one commit.

    Commit: `chore(bench): force result cache off for first-run timings (04-08)`
  </action>
  <acceptance_criteria>
    - The worker env always pins CHAT_ANALYZER_RESULT_CACHE=0 regardless of the caller's environment
  </acceptance_criteria>
  <verify>
    <automated>python -m ruff check scripts/benchmark.py && python -c "import re; s=open('scripts/benchmark.py',encoding='utf-8').read(); assert 'CHAT_ANALYZER_RESULT_CACHE' in s and '\"0\"' in s[s.index('CHAT_ANALYZER_RESULT_CACHE'):s.index('CHAT_ANALYZER_RESULT_CACHE')+40]; print('benchmark env pin ok')"</automated>
  </verify>
  <done>Worker env hard-pins the cache off; ruff clean</done>
</task>

<task type="auto">
  <name>Task 5: NEW tests/test_result_cache.py — 15 fast mocked tests</name>
  <files>tests/test_result_cache.py</files>
  <read_first>tests/test_emotion_sampling.py (`_mocked_models` 345-365, `_classifier` 96-101, `_non_tty_console` 381-385, tty `console.input` patch 408-430, transformers-None skip 53-56), 04-08-RESEARCH.md Q7 (the full recommended test list a-o)</read_first>
  <action>
    Create `tests/test_result_cache.py` — pytest style, fast (NO `slow` marker), all model callables mocked (D-17), mirroring test_emotion_sampling.py's `_mocked_models()` pattern: patch `transformers.pipeline`, the `_emotion_analyzer`/`_emotion_model_loaded` module singletons, and the T5 from_pretrained classes; skip the pipeline tests when transformers is None (the 53-56 guard). Drive the REAL `run_pipeline` (QUAL-02) with `nlp_enabled=True` on a small generated WhatsApp fixture (copy `_big_whatsapp_file`/`_small_fixture_df` helpers). Cache dir isolation: `monkeypatch.setenv("CHAT_ANALYZER_RESULT_CACHE", str(tmp_path))` per test — exercises the real env parser, parallel-safe, zero repo pollution. Headless-first: `os.environ.setdefault("MPLBACKEND", "Agg")` at module top.

    The 15 tests (research Q7 a-o — behavioral contracts verbatim):
    a. `test_cache_disabled_by_default` — no env var -> run_pipeline -> no cache dir created anywhere (assert the default dir path does not exist) and no "Loaded analysis from cache" in captured output.
    b. `test_env_parser` — absent/empty/"0"/"off"/"no" -> disabled (dir() is None); "1"/"on"/"true"/"yes" -> default dir; an explicit path -> that Path; garbage string -> that string as a dir (documented); never raises. Also assert the DEFAULT dir is never under the repo tree (privacy lock — research Pitfall 4).
    c. `test_miss_runs_and_stores` — run with mocked models + cache dir -> the entry `<key>.json` exists; the emotion analyzer mock was called (NLP stages ran).
    d. `test_hit_skips_nlp_stages` — second run, same file + config -> NLP mock call counter UNCHANGED (proves no re-run); output deep-equal to the first run.
    e. `test_hit_prints_loaded_label` — captured console output contains "Loaded analysis from cache".
    f. `test_file_edit_invalidates` — append one byte -> new hash -> miss -> mocks called again (new entry, old entry still present).
    g. `test_config_change_invalidates` — `CHAT_ANALYZER_EMOTION_SAMPLE=0` vs unset -> different entry files (distinct keys).
    h. `test_tier_change_invalidates` — `nlp_enabled=True` vs `False` -> different keys (emotion block presence in the signature).
    i. `test_sample_choice_in_key` — tty `y` vs `N` (patch `console.input` per test_emotion_sampling.py:408-430, over-cap fixture via `CHAT_ANALYZER_EMOTION_SAMPLE`) -> two entries with distinct keys, matching sample metadata each.
    j. `test_corrupt_cache_is_miss_not_crash` — write garbage text AND a `pickle.dumps` payload at the expected key path -> run succeeds, full pipeline runs, the corrupt file is deleted. (This test doubles as the never-pickle/never-eval proof: only json.load is used, an executable-format payload is inert data -> miss.)
    k. `test_schema_mismatch_is_miss` — payload with wrong `schema` -> miss (file NOT deleted).
    l. `test_sanitizer_roundtrip` — fixture payload containing numpy int64/float64/bool_/NaN/Inf/ndarray -> `json.dumps`/`loads` round-trips losslessly (NaN/Inf -> None, ndarray -> list, int64 -> int).
    m. `test_ttl_prune` — back-date an entry's mtime to -31 days -> any later store removes it (file gone after store of another key).
    n. `test_zip_transcript_selection_in_key` — unit test on `cache_key`: same file bytes + different `chosen_transcripts` lists -> different keys; also same zip bytes + different chosen lists -> different keys (the zip-level media property is captured by the whole-zip hash — assert key equality when only mtime changes).
    o. `test_report_path_reset_on_load` — store a payload with a non-empty report_path -> loaded results have `""` and the file on disk never surfaces the stale path.

    Keep tests fast and deterministic: small fixtures, no real-model inference, no subprocesses. Commit: `test(nlp): result cache hit/miss/key/corruption/TTL suite (04-08)`
  </action>
  <acceptance_criteria>
    - All 15 tests pass: `python -m pytest tests/test_result_cache.py -q` -> 15 passed, fast
    - Tests drive the REAL run_pipeline with mocked models (QUAL-02, D-17); zero `slow` markers, zero real-model inference
    - Cache dir isolation via monkeypatch per test; no test writes inside the repo tree
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_result_cache.py -q</automated>
  </verify>
  <done>15-test suite green: hit/miss, key invalidation (file/config/tier/sample/zip), corruption+schema-mismatch miss, TTL prune, sanitizer round-trip, report_path reset, opt-in default OFF</done>
</task>

<task type="auto">
  <name>Task 6: README env-var doc + privacy note + 04-08 SUMMARY</name>
  <files>README.md, .planning/phases/04-nlp-extras-quality-gate/04-08-SUMMARY.md</files>
  <read_first>README.md (env-var list at lines ~99-102 — the 04-07 CHAT_ANALYZER_EMOTION_WORKERS line is UNCOMMITTED, preserve it; Privacy section at ~133), .planning/phases/04-nlp-extras-quality-gate/04-06-SUMMARY.md (SUMMARY template)</read_first>
  <action>
    Two files:

    1. README.md:
       - Add a `CHAT_ANALYZER_RESULT_CACHE` bullet in the env-var list right after the `CHAT_ANALYZER_EMOTION_WORKERS` bullet (which is uncommitted — append after it WITHOUT rewriting it): default **OFF**; `0`/`off`/`false`/`no` disable; `1`/`on`/`true`/`yes` enable with the default directory (`%LOCALAPPDATA%\chat-analyzer\cache` on Windows, `~/.cache/chat-analyzer` elsewhere); any other value IS the cache directory. On a repeat run of the same file with the same settings, the terminal prints `[INFO] Loaded analysis from cache` and the run completes in seconds; the HTML report is always regenerated fresh. Entries expire after 30 days.
       - Privacy section: one sentence — analysis results are cached OUTSIDE the repo (never in the working tree); delete the cache directory to erase all stored analysis data (privacy self-service).
       - Also add the README note from research A2: cached results are invalidated by app-version/schema changes, but during development the version is static — a dev with the cache ON should delete the cache dir after code changes (the schema constant is the manual override).

    2. Create `.planning/phases/04-nlp-extras-quality-gate/04-08-SUMMARY.md` following the 04-06-SUMMARY.md template (frontmatter phase/plan/subsystem/tags + dependency graph + tech tracking + key-files + key-decisions + patterns + task commits + decisions/deviations/threat scan/user setup + self-check), filled with THIS plan's 6 commits, the files created/modified, the decisions pinned (default-OFF opt-in, effective-cap-in-key, report_path-blanked, json-only), and the verification results (15 cache tests green, full fast suite count, ruff clean). This is the plan's output artifact — created on completion, not stubbed.

    Commit: `docs: result cache env var + privacy note; 04-08 SUMMARY (04-08)`
  </action>
  <acceptance_criteria>
    - README documents CHAT_ANALYZER_RESULT_CACHE (default OFF, keyword/path semantics, 30-day TTL, Loaded label, fresh report) + cache-deletion privacy sentence; the uncommitted EMOTION_WORKERS bullet survives untouched
    - 04-08-SUMMARY.md exists with the 6 commits, verification results, and decisions
  </acceptance_criteria>
  <verify>
    <automated>Select-String -Path README.md -Pattern 'CHAT_ANALYZER_RESULT_CACHE','cache directory to erase all stored analysis data' | Measure-Object -Line | ForEach-Object { $_.Lines -ge 2 }; Test-Path -LiteralPath ".planning\phases\04-nlp-extras-quality-gate\04-08-SUMMARY.md"</automated>
  </verify>
  <done>README env docs + privacy note live; 04-08-SUMMARY.md committed</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| env var `CHAT_ANALYZER_RESULT_CACHE` -> pipeline | an arbitrary string value becomes a filesystem path (explicit-dir mode) |
| cache file -> pipeline load | a corrupt/tampered/expired payload crosses into the report pipeline via `run_pipeline`'s load path |
| input file bytes -> cache key | sha256 of user-controlled bytes becomes the identity of the stored payload |
| cache dir -> OS user profile | personal derived data (sender names, top words, charts) at rest outside the repo |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-20 | Tampering | cached-payload code execution | mitigate | Hard rule: `json.load` only — never pickle, never eval (research Security Domain). Hex-only key filenames (no user input in paths -> no traversal). Schema/type check on load; corrupt file unlinked; any failure = miss, never a crash. Test j stores a `pickle.dumps` payload and asserts miss-not-crash (proves json-only). |
| T-04-21 | Spoofing | cache poisoning (attacker crafts a plausible payload at the expected key path) | accept | The cache dir is user-owned (%LOCALAPPDATA% / ~/.cache) — a local attacker who can write there already has full user access; the mitigations are the opt-in default (no cache exists unless enabled) and the documented "delete the cache directory" escape hatch, not access control (research Security Domain). |
| T-04-22 | Information disclosure | personal derived data at rest | mitigate | Cache dir OUTSIDE the repo by construction (privacy lock — research Q3/Pitfall 4); opt-in only (default OFF — a clone creates nothing); README documents deletion for full erase. Payload carries derived data (participants, top words, narrative observations, base64 charts) but NEVER raw message text (verified contract shape, contracts.py:25-53). |
| T-04-23 | Tampering | env-driven arbitrary cache dir path | mitigate | Non-keyword env values are used as the cache dir verbatim (documented — A6; garbage is indistinguishable from a path); the parser never raises and never creates the dir (side-effect-free). File names are hex digests only, so even a hostile path value cannot inject into file names; os.replace confined to the resolved dir. |
| T-04-24 | Denial of service | unbounded disk growth | mitigate | 30-day mtime TTL (`RESULT_CACHE_TTL_DAYS`) pruned on every store, best-effort try/except OSError (never raises); per-entry size dominated by base64 charts (tens of MB for a handful of weekly chats — research Q5.5). |
| T-04-SC | Tampering | dependency installs | accept | ZERO new packages (stdlib hashlib/json/os/tempfile/pathlib only); Package Legitimacy Audit N/A per research — no slopcheck run required. |
</threat_model>

<verification>
- `python -m pytest tests/test_result_cache.py -q` -> 15 passed, fast, mocked (D-17), offline-safe
- `python -m pytest tests/test_phase4_nlp.py tests/test_emotion_sampling.py -q` -> green (the Task 3 relocation changed no stage label/order/prompt behavior)
- Full fast suite: `python -m pytest -m "not slow" -q` -> green (the pre-existing cp1252 smoke failure was addressed by `86faefe` — confirm it is gone; if any failure remains, it must pre-date this plan and be logged to deferred-items.md per the scope boundary rule)
- `python -m ruff check src/chat_analyzer/cli/result_cache.py src/chat_analyzer/cli/nlp_gate.py src/chat_analyzer/cli/pipeline.py src/chat_analyzer/cli/zip_input.py scripts/benchmark.py tests/test_result_cache.py` -> clean (touched files only; legacy lint debt cleared at `af89446`)
- Manual (optional, user's machine — no chat data may enter the repo): set `CHAT_ANALYZER_RESULT_CACHE=1`, run `chat-analyzer <real export>` twice; second run prints `[INFO] Loaded analysis from cache`, completes in seconds, and the report is regenerated fresh in the cwd. Also confirm `git status` stays clean of cache artifacts after runs (privacy lock).
</verification>

<success_criteria>
- SC1: A clone/user who sets `CHAT_ANALYZER_RESULT_CACHE` (keyword or dir) gets a repeat run of the same file in seconds with identical terminal + report content, an honest `[INFO] Loaded analysis from cache` label, and a report always regenerated fresh (never stale — D-09)
- SC2: With the env var unset (default OFF), behavior is byte-identical to today: all stages run, no cache dir is created anywhere, nothing new appears in git status
- SC3: File edits, tier changes, sample-cap choices, worker-count changes, and app/schema upgrades all invalidate the key -> miss -> full re-analysis (correct, never stale)
- SC4: Cache files live outside the repo tree (default `%LOCALAPPDATA%\chat-analyzer\cache` / `~/.cache/chat-analyzer`); the privacy lock holds — no personal data ships in the repo
- SC5: Corruption/schema mismatch/expired entries degrade to a miss (never a crash), corrupt files self-heal, 30-day TTL prunes on store
- SC6: All 15 cache tests pass fast with mocked models (D-17); the full fast suite stays green; ruff clean on touched files
- SC7: README documents the env var, its semantics, and the cache-deletion privacy escape hatch

## Assumptions Log + Metadata

| # | Claim | Section | Risk if Wrong |
|---|-------|---------|---------------|
| A1 | Moving the Option C sample block earlier changes no observable behavior (same prompt text, same stage order) — research A7 | Q6.3 | If wrong, `test_stage_narration_and_order` or the 04-06 sampling tests fail — caught by this plan's Task 3 verify gate |
| A2 | The effective sample cap (tty y/N collapsed) belongs in the key and the prompt is re-asked on hits — research A1 | Q1.4/Q6.3 | If a future UX drops the re-prompt, key semantics shift — the plan locks the re-prompt behavior |
| A3 | `parse_zip_with_report` returning a 4th element is additive (single caller verified by grep) | Q1.3 | No other consumer exists today; a future caller must unpack 4 values — docstring updated |
| A4 | `emotion_worker_count` in the key despite parity-identical output — research A3 | Q1.4 | Spurious misses when the user changes workers; conservative per the research question |
| A5 | App version + schema in the key invalidate on code changes; dev-mode static version can serve stale results — research A2 | Q1.4 | Mitigated by default-OFF + README deletion note; `RESULT_CACHE_SCHEMA` is the manual override |
| A6 | 30-day mtime TTL, prune-on-store — research A4 | Q5.5 | Too aggressive -> frequent misses; too lax -> disk growth; constant is trivially tunable |
| A7 | Non-keyword env value = explicit cache dir (garbage indistinguishable from a path) — research A6 | Q4 | A typo'd value creates a cache dir at that name — documented in README, never raises |
| A8 | The recursive `_sanitize` covers all current numpy leaks and any added later (central walker) | Q2.3 | New contract fields that bypass the walker would fail at store — test l guards the known 9+ fields |
| A9 | %LOCALAPPDATA% is the right Windows default (non-roaming; %APPDATA% would OneDrive-sync caches) — research Q3.3 | Q3 | Wrong-dir choice only misplaces data; explicit env path overrides; LOCALAPPDATA verified present on this box |

**Metadata:**
- **Valid until:** 2026-09-14 (analysis code at HEAD is stable; a future phase that changes the AnalysisResults contract must bump `RESULT_CACHE_SCHEMA` and re-read research Q2)
- **Pins locked:** stdlib only — hashlib/json/os/tempfile/pathlib/importlib.metadata (Python 3.14.2 in `.venv`, floor >=3.11 per pyproject); transformers pinned `<5.15` per pyproject `[nlp]` extra (04-07); pytest >=7.4 dev extra; zero new packages
- **Environment:** Windows 11 / win32; LOCALAPPDATA=`C:\Users\KIIT0001\AppData\Local` (verified); `slow` marker exists in pyproject (this plan's tests are fast, no marker needed)
- **Repo state at plan time:** HEAD `07a7b57`; README.md has an uncommitted 1-line EMOTION_WORKERS doc change (04-07); 04-07/04-08-RESEARCH.md untracked (research docs stay uncommitted per prior instruction)
- **Scope guard:** parse always runs (needed for n_scorable + honest parsed-count line); the tier menu stays in main.py (consent point — D-04); no CLI flags (D-08); no parser/analysis/adapters/render/report_html changes (payload is the existing contract)
</success_criteria>

<output>
Create `.planning/phases/04-nlp-extras-quality-gate/04-08-SUMMARY.md` when done (Task 6 — the plan's output artifact)
</output>