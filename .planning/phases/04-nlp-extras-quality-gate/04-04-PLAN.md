---
phase: 04-nlp-extras-quality-gate
plan: 04
type: execute
wave: 4
depends_on: ["04-03"]
files_modified:
  - tests/test_analysis.py
  - tests/test_parser.py
autonomous: true
requirements: [QUAL-02]
must_haves:
  truths:
    - "Legacy analysis tests import and call the REAL chat_analyzer.* modules on small fixture DataFrames — no duplicated-logic copies (D-16)"
    - "Legacy parser tests import and call the REAL WhatsAppParser / Telegram parse functions on inline fixture exports (D-16)"
    - "Heavy model callables (transformers pipeline, model download) are mocked with unittest.mock; the REAL analyzer/pipeline logic is what the tests exercise (D-17)"
    - "The legacy files keep the unittest framework (D-16 — no new test framework); the new test_phase4_* files are pytest-style (framework tension resolved: in-place rewire = unittest, new coverage = pytest)"
    - "The full test suite (legacy unittest + phase 2/4 pytest) passes in one command"
  artifacts:
    - path: "tests/test_analysis.py"
      provides: "rewired real-module analysis tests"
      contains: "from chat_analyzer.analysis"
    - path: "tests/test_parser.py"
      provides: "rewired real-module parser tests"
      contains: "from chat_analyzer.parser"
  key_links:
    - from: "tests/test_analysis.py"
      to: "chat_analyzer.analysis.eda.ChatEDA"
      via: "fixture DataFrame → real generate_comprehensive_summary"
      pattern: "ChatEDA\\(df\\)"
    - from: "tests/test_analysis.py"
      to: "chat_analyzer.analysis.emotion.EmotionAnalyzer"
      via: "mocked transformers.pipeline (D-17)"
      pattern: "mock.patch\\(.*transformers.pipeline"
    - from: "tests/test_parser.py"
      to: "chat_analyzer.parser.whatsapp_parser.WhatsAppParser"
      via: "inline fixture export string"
      pattern: "WhatsAppParser\\(\\)"
---

<objective>
Close the QUAL-02 quality gate: rewire the legacy duplicated-logic unittest files (`tests/test_analysis.py`, `tests/test_parser.py`) so they import and exercise the REAL `chat_analyzer.*` modules with small fixture DataFrames, keep the unittest framework per D-16, mock only the heavy model callables per D-17, and prove the entire suite (legacy + phase 2 + phase 4) passes in one command.

Purpose: The legacy tests currently re-implement analysis logic by hand (e.g., mock score lists, hand-rolled pandas math) — they assert the tests' own copy of the logic, not the product. D-16 mandates rewiring; D-17 keeps the suite fast and offline-safe. This plan is the terminal quality gate of the phase.

**Plan slice story:** **As a** maintainer, **I want to** see the legacy tests exercise the real modules and the whole suite pass in one command, **so that** the quality gate proves the shipped pipeline actually works.

**Framework tension resolution (reconciliation note #5):** D-16's "keep unittest" applies to the LEGACY files being rewired in place (`test_analysis.py`, `test_parser.py` stay unittest, aggregators kept). The NEW `test_phase4_*.py` files created in plans 04-01/02/03 are pytest-style, consistent with the existing `test_phase2_*.py` (RESEARCH Open Question 3 recommendation). pytest discovers both styles in one run — no conflict.
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-nlp-extras-quality-gate/04-CONTEXT.md  (D-16, D-17 locked; discretion: exact test-file organization)
@.planning/phases/04-nlp-extras-quality-gate/04-RESEARCH.md  (D-16/D-17 spec lines 21-22; Code Examples 220-230 faithful mock; Open Question 3)
@.planning/phases/04-nlp-extras-quality-gate/04-PATTERNS.md  (test_analysis.py rewire section lines 350-370; keep run_analysis_tests aggregator; fixture-DataFrame pattern from test_phase2_pipeline.py:97-113)
@.planning/codebase/TESTING.md  (legacy conventions, run_*_tests() aggregators)
@tests/test_analysis.py
@tests/test_parser.py
@tests/test_phase2_pipeline.py
@src/chat_analyzer/ingest/ingestion.py  (messages_to_dataframe — fixture DataFrame builder)

<interfaces>
Fixture-DataFrame pattern (test_phase2_pipeline.py:97-113 — the canonical real-module test shape):
- Build: `df = messages_to_dataframe([{"datetime": "2025-09-15T09:45:00", "sender": "A", "message": "just me here"}])`
- Then call real modules: `ChatEDA(df)`, `add_sentiment_analysis(df)`, `analyze_relationship_health(df)`, etc.

Real-module import paths to use (do NOT import via analysis/__init__ re-exports — CONVENTIONS.md:165 stale re-export warning, PATTERNS reconciliation note #3):
- `from chat_analyzer.analysis.eda import ChatEDA`
- `from chat_analyzer.analysis.sentiment import add_sentiment_analysis, get_sentiment_summary` (+ pin `_sentiment.TRANSFORMERS_AVAILABLE = False` per test_phase2_pipeline.py:85,95)
- `from chat_analyzer.analysis.emotion import EmotionAnalyzer` (with transformers.pipeline mocked — D-17)
- `from chat_analyzer.analysis.relationship_health import analyze_relationship_health`
- `from chat_analyzer.parser.whatsapp_parser import WhatsAppParser`
- `from chat_analyzer.parser.telegram_parser import parse_telegram_chat_with_report`

Faithful emotion mock (D-17, RESEARCH lines 220-230): patch `chat_analyzer.analysis.emotion.transformers.pipeline` with a flat list-of-dicts `[{"label": "joy", "score": 0.87}, ...]` — NEVER mock EmotionAnalyzer itself.

Existing legacy aggregators to KEEP (test_analysis.py lines 381-397): `run_analysis_tests()` and `if __name__ == "__main__": unittest.main()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewire test_analysis.py to real modules (unittest, mocked heavy callables)</name>
  <files>tests/test_analysis.py</files>
  <read_first>tests/test_analysis.py (full file — the 7 Test* classes and run_analysis_tests aggregator), tests/test_phase2_pipeline.py (lines 85-116 the real-module + _AVAILABLE pin pattern), src/chat_analyzer/analysis/emotion.py (post-04-02 state: analyze_emotions/get_emotion_summary signatures), src/chat_analyzer/analysis/relationship_health.py (analyze_relationship_health at 1071)</read_first>
  <action>
    Rewire in place per D-16/D-17. Keep the unittest framework, class structure (7 Test* classes), and the run_analysis_tests() aggregator. Replace every duplicated-logic assertion with a call to the REAL module on a small fixture DataFrame:

    1. TestEDAModule — keep the setUp fixture DataFrame (dates/sender/message/message_length); replace the hand-rolled pandas re-implementations with real calls: `eda = ChatEDA(self.test_df)`; assert `eda.analyze_message_volume()["sender_counts"]` matches expected counts; `eda.generate_comprehensive_summary()` contains key sections; hourly activity from `eda.analyze_message_volume()["hourly_activity"]` (import `from chat_analyzer.analysis.eda import ChatEDA`).
    2. TestSentimentAnalysis — import `add_sentiment_analysis, get_sentiment_summary` from chat_analyzer.analysis.sentiment; in setUp, build a small DataFrame from the positive/negative/neutral message lists and run `add_sentiment_analysis(df)`; assert the real `vader_sentiment`/`vader_compound` columns exist and positive messages have compound > 0, negative < 0, neutral near 0 (use assertGreater/assertLess). Before calling, pin `from chat_analyzer.analysis import sentiment as _sentiment; _sentiment.TRANSFORMERS_AVAILABLE = False` (VADER path, same as the pipeline — test_phase2_pipeline.py:85,95).
    3. TestEmotionClassification — import EmotionAnalyzer from chat_analyzer.analysis.emotion; wrap the analyzer calls in `unittest.mock.patch("chat_analyzer.analysis.emotion.transformers.pipeline", return_value=<faithful list-of-dicts>)` (D-17: mock the callable, drive the real method); run `analyzer.analyze_emotions(df)` on a 2-row fixture and assert: all 6 `emotion_*` columns exist, scores are NOT all uniform (the real parse fix surfaces), `get_emotion_summary(df_emo)["emotion_distribution"]` is a dict. Assert the model name default is the locked `bhadresh-savani/distilbert-base-uncased-emotion` via `EmotionAnalyzer().model_name`.
    4. TestRelationshipHealth — import `analyze_relationship_health` from chat_analyzer.analysis.relationship_health; call it on the existing setUp 2-sender DataFrame; assert `health["health_score"]["overall_health_score"]` is a float in [0,1], `health["health_score"]["grade"]` is a str, `health["conversation_stats"]["total_messages"] == 100`, and `health["initiator_analysis"]["balance_score"]` is a float.
    5. TestGamificationFeatures — same real call; assert `health["friendship_index"]["total_score"]`-style keys exist via defensive `.get()` access (read the actual key names from relationship_health.py first — lines 501+; use whatever scalar key exists); assert streaks/milestones keys present in the result dict.
    6. TestRollingHealthScore — call the real `calculate_rolling_health_score` (import from chat_analyzer.analysis.relationship_health) on the existing daily DataFrame; assert the returned structure contains per-window scores (read the actual return shape first at relationship_health.py:379-438 and assert on real keys).
    7. TestVisualizationIntegration — replace the hand-rolled resample/heatmap math with real figure calls: `ChatVisualizer().plot_message_timeline(df)` and `plot_activity_heatmap(df)` (from chat_analyzer.utils.visualization) return matplotlib Figure objects (assert `fig is not None` and `type(fig).__name__ == "Figure"`). This asserts the base64-embeddable contract the report depends on.
    8. Keep `run_analysis_tests()` exactly as-is (aggregator over the 7 classes) and the `if __name__ == "__main__"` block. Update the module docstring to state the tests exercise real chat_analyzer.* modules (D-16).
    Run the file with unittest AND pytest; both must pass.
  </action>
  <acceptance_criteria>
    - test_analysis.py imports from `chat_analyzer.analysis` / `chat_analyzer.utils` (grep: `from chat_analyzer` appears ≥ 5 times)
    - No hand-mocked score lists remain (grep for `positive_scores = ` / `mock scores` — absent)
    - Emotion tests use `mock.patch("chat_analyzer.analysis.emotion.transformers.pipeline", ...)` and assert non-uniform scores
    - `run_analysis_tests()` aggregator still present
    - `python -m unittest tests.test_analysis -v` passes AND `python -m pytest tests/test_analysis.py -x` passes (both collection styles green)
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_analysis -v && python -m pytest tests/test_analysis.py -x</automated>
  </verify>
  <done>Legacy analysis tests exercise the real modules with mocked heavy callables, in unittest, aggregator intact</done>
</task>

<task type="auto">
  <name>Task 2: Rewire test_parser.py to real parser modules (unittest)</name>
  <files>tests/test_parser.py</files>
  <read_first>tests/test_parser.py (full file — 3 unittest classes), src/chat_analyzer/parser/whatsapp_parser.py (WhatsAppParser.parse_file_with_report signature + return shape), src/chat_analyzer/parser/telegram_parser.py (parse_telegram_chat_with_report signature + return shape), tests/test_phase2_whatsapp.py + tests/test_phase2_telegram.py (real-parser assertion patterns to mirror)</read_first>
  <action>
    Rewire in place per D-16. Keep unittest and the 3 class structure. Replace every duplicated-logic parse with the REAL parser on an inline fixture export:

    1. TestWhatsAppParser — write an inline fixture export string (3-4 real WhatsApp lines, e.g. `1/15/2024, 10:00 AM - Alice: hello` + one multiline + one system message) to a tmp file (tempfile.NamedTemporaryFile or unittest's tempfile, mirroring test_end_to_end.py's setUp pattern), then `WhatsAppParser().parse_file_with_report(path)` (import `from chat_analyzer.parser.whatsapp_parser import WhatsAppParser`). Assert: rows count matches the real expected parsed messages; system message excluded; sender/datetime/message columns populated; the parse report counters (parsed_messages/skipped_lines) match the fixture. For the datetime-format edge assertions, feed a regional format line and assert it parses to the expected naive UTC datetime (Phase 2's strict-parse behavior).
    2. TestTelegramParser — inline Telegram JSON dict (2-3 messages with id/date/from/text + one service message) written to a tmp .json file; call `parse_telegram_chat_with_report(path)` (import `from chat_analyzer.parser.telegram_parser import parse_telegram_chat_with_report`); assert message count, service-message exclusion, sender/text extraction, datetime conversion.
    3. TestParserEdgeCases — reuse the real parsers on inline fixtures for: sender names with special characters, unicode content, very long messages, and empty-file handling (assert the friendly skip/zero behavior, not a crash).
    4. Keep any module-level helpers and the `if __name__ == "__main__"` block. Do NOT import via parser/__init__ re-exports (CONVENTIONS.md:165 warning).
    Run the file with unittest AND pytest; both must pass.
  </action>
  <acceptance_criteria>
    - test_parser.py imports `WhatsAppParser` and `parse_telegram_chat_with_report` from `chat_analyzer.parser`
    - No duplicated parsing logic remains (grep for `split(` on raw lines / hand-built parse regexes — absent)
    - `python -m unittest tests.test_parser -v` passes AND `python -m pytest tests/test_parser.py -x` passes
  </acceptance_criteria>
  <verify>
    <automated>python -m unittest tests.test_parser -v && python -m pytest tests/test_parser.py -x</automated>
  </verify>
  <done>Legacy parser tests exercise the real parsers on inline fixtures, in unittest</done>
</task>

<task type="auto">
  <name>Task 3: Full-suite green check (terminal quality gate)</name>
  <files>tests/test_analysis.py, tests/test_parser.py</files>
  <read_first>tests/ (inventory: test_phase1_smoke.py, test_phase2_*.py, test_phase4_*.py, rewired legacy files), pyproject.toml (dev extra has pytest + ruff)</read_first>
  <action>
    Terminal gate for QUAL-02. Do NOT create new tests here — verify everything:

    1. Run the FULL suite: `python -m pytest` from the repo root (collects unittest-style legacy files + pytest-style phase 2/4 files in one discovery — both frameworks in one run, resolving the framework tension). All tests must pass.
    2. Run the unittest aggregators explicitly: `python -m unittest tests.test_analysis tests.test_parser -v` (proves the legacy framework path the README/dev workflow may use).
    3. Lint: `python -m ruff check src/chat_analyzer tests` — 0 errors.
    4. If any failure surfaces from earlier plans' interactions (e.g., a phase-4 test asserting an adapter key that another plan changed), fix the ASSERTION in the failing test file to match the shipped contract — do not weaken the production behavior.
    5. Record the suite count (pytest summary line) in the plan SUMMARY output for STATE.md.
  </action>
  <acceptance_criteria>
    - `python -m pytest` exits 0 with all collected tests passing (legacy + phase 2 + phase 4)
    - `python -m unittest tests.test_analysis tests.test_parser -v` passes
    - `python -m ruff check src/chat_analyzer tests` passes (0 errors)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest && python -m unittest tests.test_analysis tests.test_parser -v && python -m ruff check src/chat_analyzer tests</automated>
  </verify>
  <done>Entire suite green in one command — the QUAL-02 gate closes</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| tests → real modules | rewired tests execute the shipped chat_analyzer.* code |
| tests → mocked transformers | heavy callables are patched at the import site; no real model download in the suite |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Tampering | test suite integrity | mitigate | D-17 mocks patch ONLY `transformers.pipeline`/from_pretrained symbols — the real analyzer/pipeline logic is what assertions exercise; no test may mock EmotionAnalyzer itself (RESEARCH lines 230) |
| T-04-15 | Denial of Service | suite runtime / offline CI | mitigate | no real model construction or download in tests — the suite is fast and offline-safe (D-17); `_sentiment.TRANSFORMERS_AVAILABLE = False` pins the VADER path in sentiment tests |
| T-04-16 | Information Disclosure | test fixtures | mitigate | fixtures are inline strings / small DataFrames — no real chat exports or user data enter the repo |
| T-04-SC | Tampering | dependency installs | accept | no new packages; pytest/ruff already in `[dev]` extra (pyproject.toml:28) |
</threat_model>

<verification>
- `python -m pytest` — 0 failures (full collection: legacy unittest classes + phase 2/4 pytest files)
- `python -m unittest tests.test_analysis tests.test_parser -v` — 0 failures
- `python -m ruff check src/chat_analyzer tests` — 0 errors
- grep evidence: test_analysis.py imports real chat_analyzer modules; emotion tests patch transformers.pipeline only
</verification>

<success_criteria>
- QUAL-02 satisfied: tests exercise the real `chat_analyzer.*` modules (parse → analyze → render pipeline covered by phase 4 e2e tests; legacy files rewired to real modules)
- D-16 honored: unittest retained in the legacy files, aggregators intact, no new test framework
- D-17 honored: heavy model callables mocked; suite runs fast, offline-safe, without the `[nlp]` extra installed
- Full suite green in one command — the terminal quality gate
</success_criteria>

<output>
Create `.planning/phases/04-nlp-extras-quality-gate/04-04-SUMMARY.md` when done
</output>
