---
phase: 04-nlp-extras-quality-gate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/chat_analyzer/cli/contracts.py
  - src/chat_analyzer/analysis/relationship_health.py
  - src/chat_analyzer/analysis/network_graph.py
  - src/chat_analyzer/cli/adapters.py
  - src/chat_analyzer/cli/pipeline.py
  - src/chat_analyzer/cli/report_html.py
  - tests/test_phase4_alwayson.py
  - tests/test_phase2_cli.py
  - tests/test_phase2_report.py
  - tests/test_phase2_pipeline.py
autonomous: true
requirements: [ANAL-07, ANAL-09, OUT-04, OUT-05, QUAL-02]
must_haves:
  truths:
    - "User runs chat-analyzer on any export and the HTML report contains Relationship Health and Network tabs"
    - "Relationship health score + grade appear in the report with no [nlp] extra installed (D-07)"
    - "Network graph figure + metrics appear in the report with no [nlp] extra installed (D-07b)"
    - "The report always lands in the current working directory as <chat_name>_report.html (D-08/D-09)"
    - "Phase 2 tests still pass after the report-location change (reconciliation note #3)"
  artifacts:
    - path: "src/chat_analyzer/cli/contracts.py"
      provides: "health + network keys in AnalysisResults"
      contains: "health: dict[str, Any]"
    - path: "src/chat_analyzer/analysis/network_graph.py"
      provides: "figure-returning network wrapper"
      contains: "def network_figure"
    - path: "src/chat_analyzer/analysis/relationship_health.py"
      provides: "import-hygiene fix (NullHandler, no logging.basicConfig)"
      not_contains: "logging.basicConfig"
    - path: "src/chat_analyzer/cli/report_html.py"
      provides: "health + network tabs, cwd report location"
      contains: "id=\"tab-health\""
    - path: "tests/test_phase4_alwayson.py"
      provides: "e2e proof of the vertical slice"
      exports: ["test_health_network_in_report"]
  key_links:
    - from: "src/chat_analyzer/cli/pipeline.py"
      to: "chat_analyzer.analysis.relationship_health.analyze_relationship_health"
      via: "always-on stage in run_pipeline"
      pattern: "analyze_relationship_health\\(df\\)"
    - from: "src/chat_analyzer/cli/pipeline.py"
      to: "chat_analyzer.analysis.network_graph.network_figure"
      via: "chart dict entry 'network'"
      pattern: "network_figure\\(df\\)"
    - from: "src/chat_analyzer/cli/adapters.py"
      to: "AnalysisResults['health']"
      via: "defensive .get() extractors"
      pattern: "health_block"
    - from: "src/chat_analyzer/cli/report_html.py"
      to: "Path.cwd()"
      via: "report_path = Path.cwd() / f\"{stem}_report.html\""
      pattern: "Path\\.cwd\\(\\)"
---

<objective>
Wire the two always-on NLP-family insights — Relationship Health (ANAL-07) and Network Graph (ANAL-09) — end-to-end: analysis modules → adapters → pipeline → HTML report, with the report location moved to the current working directory (D-08/D-09).

Purpose: Per D-07/D-07b these features need no torch/transformers (pandas/numpy/networkx/matplotlib only), so gating them behind `[nlp]` adds friction with zero benefit. This plan makes the lean install strictly richer. It also delivers the OUT-04/OUT-05 resolution: **no flags** — the report is the deliverable, always generated, always saved to cwd.

Output: Health + Network report tabs; report at `Path.cwd() / <stem>_report.html`; Phase 2 tests reconciled to the new location; e2e proof test.

**Phase goal (derived from ROADMAP goal line — ROADMAP wording is not user-story format; story synthesized from its capability content, suggest running `/gsd mvp-phase 04` for the canonical story):**
**As a** chat export user, **I want to** run one command and get deep NLP insights in an HTML report that auto-opens, **so that** I understand my conversation without learning any flags or install steps.

**Plan slice story:** **As a** chat export user, **I want to** see Relationship Health and Network tabs in my report with no extra install, **so that** I get deeper insights from a lean install.
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-nlp-extras-quality-gate/04-CONTEXT.md  (D-07, D-07b, D-08, D-09, D-10, D-11 locked)
@.planning/phases/04-nlp-extras-quality-gate/04-RESEARCH.md  (Pattern 2 wrappers, Pattern 3 adapter extraction, Pitfall 3 logging)
@.planning/phases/04-nlp-extras-quality-gate/04-PATTERNS.md  (analog map + reconciliation notes 1, 2, 3, 5)
@.planning/codebase/CONVENTIONS.md  (defensive .get() adapters, NullHandler logging)
@src/chat_analyzer/cli/report_html.py
@src/chat_analyzer/cli/adapters.py
@src/chat_analyzer/cli/pipeline.py
@tests/test_phase2_cli.py
@tests/test_phase2_report.py
@tests/test_phase2_pipeline.py

<interfaces>
From src/chat_analyzer/cli/contracts.py (current — Task 2 extends):
```python
class AnalysisResults(TypedDict):
    source: str
    parse: dict[str, int]
    stats: dict[str, Any]
    participants: dict[str, Any]
    content: dict[str, Any]
    sentiment: dict[str, Any]
    charts: dict[str, str]
    insights: list[str]
    report_path: str
```

From src/chat_analyzer/cli/adapters.py (current — Task 2 extends; new params MUST have defaults):
```python
def adapt(source, parse: ParseReport, df, summary, volume, dynamics, content,
          sentiment, charts) -> AnalysisResults:
```

From src/chat_analyzer/analysis/relationship_health.py (VERIFIED shapes, consumed by the adapter):
- `analyze_relationship_health(df)` returns dict with keys: `conversation_stats`, `initiator_analysis` (has `balance_score`, `total_conversations`), `response_analysis` (has `response_balance_score`, `overall_avg_response_minutes`), `dominance_analysis` (has `composite_dominance_score`, `message_count_balance`, `message_length_balance`), `health_score` (has `overall_health_score` float, `grade` str, `components` dict), `prepared_data` (DataFrame — NEVER serialize), plus gamification keys.
- Import hygiene: line 24 is `logging.basicConfig(level=logging.INFO)` — must become NullHandler.

From src/chat_analyzer/analysis/network_graph.py (VERIFIED):
- `analyze_network(df)` returns dict: `graph` (networkx.DiGraph — NEVER serialize), `metrics` (has `density`), `key_participants`, `patterns`, `subgroups`.
- `plot_network_graph` / `plot_network_dashboard` end in `plt.show()` and return None — Task 2 adds a figure-returning wrapper.

From src/chat_analyzer/utils/visualization.py (VERIFIED figure-returning convention to match):
- `ChatVisualizer.plot_relationship_health_trend(df)` returns a Figure (no plt.show()) — reuse directly for the health chart.

From src/chat_analyzer/cli/pipeline.py (current):
- `run_pipeline(path, console)` — stages: "Parsing chat", "Computing insights" (redirect_stdout block), then `adapt(...)`.
- Helpers to reuse: `stage_status(console, label)`, `fig_to_data_uri(fig)`, `_safe_chart(fig)`.

Report location change target (report_html.py line 191):
- `report_path = input_path.parent / f"{stem}_report.html"` → `report_path = Path.cwd() / f"{stem}_report.html"`
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Failing e2e test for health + network report slice</name>
  <files>tests/test_phase4_alwayson.py</files>
  <read_first>tests/test_phase2_cli.py (the _run/_copy_sample helper pattern, BROWSER=__none__, cwd param), tests/test_phase2_pipeline.py (in-process run_pipeline pattern + fixture DataFrames), src/chat_analyzer/cli/report_html.py (current tab ids)</read_first>
  <behavior>
    - Test A (in-process): run_pipeline(data/sample_chats/whatsapp_sample.txt, Console) → results["health"]["overall_score"] is a float between 0 and 1, results["network"]["density"] is a float, and "health" + "network" are keys of results["charts"] with data:image/png;base64 prefixes.
    - Test B (subprocess, cwd=tmp_path): CLI run on a copied sample → report exists at tmp_path/whatsapp_sample_report.html (cwd location, D-09), HTML contains id="tab-health" and id="tab-network" and the lead sentence about the health grade.
  </behavior>
  <action>
    Create tests/test_phase4_alwayson.py (pytest style, matching test_phase2_pipeline.py). Copy the `_run` helper shape from test_phase2_cli.py (env["BROWSER"]="__none__", utf-8, timeout=300, cwd param) and the `_copy_sample` helper (data/sample_chats/whatsapp_sample.txt → tmp_path). Test A calls run_pipeline directly with a rich Console(file=io.StringIO(), force_terminal=False); it MUST currently fail because results["health"]/["network"] do not exist yet (KeyError). Test B runs the console script with cwd=tmp_path and asserts the report HTML exists at tmp_path / "whatsapp_sample_report.html" and contains id="tab-health", id="tab-network". Do NOT implement production code in this task — the test file alone is the deliverable. Run it and confirm RED (KeyError on health / missing tab ids).
  </action>
  <acceptance_criteria>
    - tests/test_phase4_alwayson.py exists with both tests defined
    - Running `python -m pytest tests/test_phase4_alwayson.py -x` fails for the reason the features are absent (KeyError on results["health"] / report lacks tab-health)
    - No production source file was modified in this task
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_phase4_alwayson.py -x; expect FAIL (RED) — that is the success condition for this task</automated>
  </verify>
  <done>Failing e2e tests exist proving the slice is not yet delivered</done>
</task>

<task type="auto">
  <name>Task 2: Thinnest slice — contracts, analysis fixes, adapters, pipeline, report tabs, cwd location</name>
  <files>src/chat_analyzer/cli/contracts.py, src/chat_analyzer/analysis/relationship_health.py, src/chat_analyzer/analysis/network_graph.py, src/chat_analyzer/cli/adapters.py, src/chat_analyzer/cli/pipeline.py, src/chat_analyzer/cli/report_html.py</files>
  <read_first>src/chat_analyzer/cli/contracts.py (current TypedDict), src/chat_analyzer/analysis/relationship_health.py (lines 21-26 the logging landmine; lines 1071-1128 analyze_relationship_health return shape), src/chat_analyzer/analysis/network_graph.py (analyze_network at 473-505, plot_network_graph at 266-359), src/chat_analyzer/cli/adapters.py (adapt signature + defensive .get() style + build_insights), src/chat_analyzer/cli/pipeline.py (run_pipeline + redirect block + charts dict), src/chat_analyzer/cli/report_html.py (TEMPLATE nav/panels + write_report location line 191 + chart whitelist), src/chat_analyzer/utils/visualization.py (plot_relationship_health_trend figure-returning convention lines 502-556)</read_first>
  <action>
    Per D-07/D-07b/D-09/D-11. Six files, one vertical slice:

    1. contracts.py — add two keys to AnalysisResults: `health: dict[str, Any]` and `network: dict[str, Any]` (place after `sentiment`). Update the class docstring. Do NOT touch ParseReport.
    2. relationship_health.py line 24 — replace `logging.basicConfig(level=logging.INFO)` with `logging.getLogger(__name__).addHandler(logging.NullHandler())` (exact canonical shape from utils/visualization.py lines 18-20). Keep `import logging` and the `logger = logging.getLogger(__name__)` line. Do NOT modify analyze_relationship_health or any other function.
    3. network_graph.py — add a module-level function `network_figure(df) -> "matplotlib.figure.Figure"`: import matplotlib.pyplot inside, call `analyze_network(df)`, build `fig, ax = plt.subplots(figsize=(10, 8))`, `pos = nx.spring_layout(G, seed=42)`, `nx.draw_networkx(G, pos, ax=ax)`, `ax.set_title("Conversation Network")`, `return fig`. NO plt.show() (Pitfall 6 — the figure must be base64-embeddable). Reuse the existing build_interaction_network/analyze_network — do not rewrite analysis.
    4. adapters.py — extend `adapt(...)` signature with four new keyword-only params with None defaults: `health=None, network=None, emotion=None, summary=None` (defaults keep Phase 2 direct-call tests green — reconciliation note #2). Build `health_block: dict` extracting ONLY serializable scalars from the relationship-health result via defensive .get(): `overall_score` ← health["health_score"].get("overall_health_score"), `grade` ← health["health_score"].get("grade"), `components` ← health["health_score"].get("components") or {}, `initiator_balance` ← health["initiator_analysis"].get("balance_score"), `total_conversations` ← health["initiator_analysis"].get("total_conversations"), `avg_response_minutes` ← health["response_analysis"].get("overall_avg_response_minutes"), `response_balance` ← health["response_analysis"].get("response_balance_score"), `composite_dominance` ← health["dominance_analysis"].get("composite_dominance_score"). Build `network_block: dict` from the network result: `node_count`, `edge_count`, `density` ← metrics.get("density"), `reciprocity` ← metrics.get("reciprocity"), `strongest_connections` ← patterns.get("strongest_connections"), `key_participants` (scalar subset only), `subgroup_count` ← len(subgroups). NEVER leak the prepared_data DataFrame or the networkx DiGraph into AnalysisResults (Pattern 3). Add both blocks to the returned AnalysisResults when the respective arg is not None (use `health_block if health is not None else None`-style guards so basic runs get None). Extend build_insights: raise the return cap from 7 to 11 and append two lead-in sentences: one from the health grade (e.g., f"This conversation scores {grade} — overall health {score:.2f}." only when score present), one from the network (density/strongest connection, only when present). Never the string "None".
    5. pipeline.py — inside run_pipeline's "Computing insights" stage, after the existing sentiment block and still inside the redirect_stdout context, add two always-on analysis calls: `from chat_analyzer.analysis.relationship_health import analyze_relationship_health; health_res = analyze_relationship_health(df)` and `from chat_analyzer.analysis.network_graph import analyze_network; network_res = analyze_network(df)`. Extend the charts dict with `"health": _safe_chart(viz.plot_relationship_health_trend(df))` and `"network": _safe_chart(network_figure(df))` (import network_figure lazily inside the block). Pass `health=health_res, network=network_res` to the adapt(...) call at the end (emotion/summary stay None for now — they arrive in plan 04-02). Keep `_sentiment.TRANSFORMERS_AVAILABLE = False` pin, the "Parsed N messages" narration, and the smoke-contract token untouched.
    6. report_html.py — (a) D-09 location: change line 191 `report_path = input_path.parent / f"{stem}_report.html"` to `report_path = Path.cwd() / f"{stem}_report.html"`; update the module docstring line ("next to the input" → "in the current working directory") and the write_report docstring accordingly. (b) TEMPLATE: add two nav buttons after Sentiment: `<button class="tab" data-tab="health" onclick="showTab('health')">Relationship Health</button>` and `<button class="tab" data-tab="network" onclick="showTab('network')">Network</button>`; add two panels in the same .card style: id="tab-health" with `<p class="lead">{{ insights[5] }}</p>`, `{% if charts.health %}<img class="chart" alt="Relationship health trend" src="{{ charts.health }}">{% endif %}`, and a table showing overall score, grade, initiator balance, avg response minutes (guard `{% if health %}` around the panel content); id="tab-network" with `<p class="lead">{{ insights[6] }}</p>`, `{% if charts.network %}<img class="chart" alt="Conversation network" src="{{ charts.network }}">{% endif %}`, and a table of node/edge/density/strongest-connection when present. (c) write_report render call: pass `health=results.get("health", {})` and `network=results.get("network", {})` (defensive — crafted Phase 2 dicts must not KeyError). Keep _CHART_PREFIX whitelist, autoescape, sanitize_filename, utf-8 write, open_report degrade (T-02-01 boundary).
  </action>
  <acceptance_criteria>
    - contracts.py contains `health: dict[str, Any]` and `network: dict[str, Any]`
    - relationship_health.py contains `addHandler(logging.NullHandler())` and no `logging.basicConfig` (grep -v '^#' guard)
    - network_graph.py contains `def network_figure` and no `plt.show()` inside it
    - adapters.py `adapt(` signature has `health=None, network=None, emotion=None, summary=None`
    - report_html.py contains `report_path = Path.cwd() / f"{stem}_report.html"`, `id="tab-health"`, `id="tab-network"`, `charts.health`, `charts.network`
    - pipeline.py passes health= and network= into adapt(...)
    - `python -m pytest tests/test_phase4_alwayson.py -x` passes (GREEN)
    - `python -m pytest tests/test_phase1_smoke.py -x` still passes (analysis core + smoke contract unaffected)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_phase4_alwayson.py tests/test_phase1_smoke.py -x</automated>
  </verify>
  <done>Health + Network tabs render from real analysis modules; report lands in cwd; smoke contract intact</done>
</task>

<task type="auto">
  <name>Task 3: Reconcile Phase 2 tests for the cwd location, new chart keys, and new report sections</name>
  <files>tests/test_phase2_cli.py, tests/test_phase2_report.py, tests/test_phase2_pipeline.py</files>
  <read_first>tests/test_phase2_cli.py (test_report_written_next_to_input at 112-127, test_report_card_wellformed at 130-140, test_skip_surfacing at 202-223), tests/test_phase2_report.py (test_report_location_next_to_input at 122-125, _results() dict at 21-70), tests/test_phase2_pipeline.py (test_whatsapp_e2e charts-set assertion at line 42, test_single_message_no_response_time adapt() direct call at 109-113)</read_first>
  <action>
    Reconciliation note #3 + #2 + #5. Three test files, three exact fixes:

    1. test_phase2_cli.py — report-location tests must run the CLI with cwd=tmp_path (report now goes to cwd per D-09):
       - test_report_written_next_to_input: add `cwd=tmp_path` to the `_run(...)` call; the report assertion `report = tmp_path / "whatsapp_sample_report.html"` then still holds (tmp_path is both input dir and cwd); keep the REPO_ROOT.rglob before/after no-repo-write assertion.
       - test_report_card_wellformed: add `cwd=tmp_path` to `_run(...)` (it reads the HTML from tmp_path).
       - test_skip_surfacing: add `cwd=tmp_path` to `_run(...)` (it asserts report at tmp_path / "mixed_report.html").
       - Do NOT change test_interactive_path or the error-path tests (no report-location assertions there; the interactive menu arrives in plan 04-03 and is TTY-gated, so piped runs are unaffected).
    2. test_phase2_report.py — (a) test_report_location_next_to_input: monkeypatch.chdir(tmp_path) before calling _write (report now resolves against Path.cwd()), assert p == tmp_path / "chat_analysis_test_report.html". (b) _results() dict: add `"health": {"overall_score": 0.82, "grade": "Good", "components": {}, "initiator_balance": 0.6, "avg_response_minutes": 12, "response_balance": 0.7, "composite_dominance": 0.5, "total_conversations": 3}` and `"network": {"node_count": 2, "edge_count": 1, "density": 0.5, "reciprocity": 0.0, "strongest_connections": None, "key_participants": {}, "subgroup_count": 1}` so the new tabs render real content in the crafted-dict tests.
    3. test_phase2_pipeline.py — (a) test_whatsapp_e2e line 42: replace `assert set(results["charts"]) == {"timeline", "activity", "participants", "sentiment"}` with a pinned-gate assertion: monkeypatch or unittest.mock.patch chat_analyzer.cli.pipeline's nlp gate availability to False (patch `chat_analyzer.cli.nlp_gate.nlp_available` return False — the gate module does not exist until 04-02, so if it is absent this test must instead patch the module path AFTER 04-02; for THIS plan, assert the deterministic 6-key set: `{"timeline", "activity", "participants", "sentiment", "health", "network"}` and add a comment that plan 04-02 extends it to 7 keys with the gate pinned True). (b) test_single_message_no_response_time: no change needed — the new adapt() params have None defaults (reconciliation note #2 resolved by design). Verify no edit was required for (b).
  </action>
  <acceptance_criteria>
    - test_phase2_cli.py has cwd=tmp_path on the three report-location _run calls
    - test_phase2_report.py uses monkeypatch.chdir(tmp_path) in test_report_location_next_to_input and _results() contains "health" and "network" keys
    - test_phase2_pipeline.py charts assertion expects the 6-key always-on set
    - `python -m pytest tests/test_phase2_cli.py tests/test_phase2_report.py tests/test_phase2_pipeline.py -x` passes
    - `python -m ruff check src/chat_analyzer tests` passes (0 errors)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_phase2_cli.py tests/test_phase2_report.py tests/test_phase2_pipeline.py -x && python -m ruff check src/chat_analyzer tests</automated>
  </verify>
  <done>All Phase 2 tests green under the new cwd location and extended contracts</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| chat export file → pipeline | untrusted message content crosses into pandas DataFrame and analysis |
| analysis results → HTML | chat-derived strings cross into the jinja2 template |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | report_html.py TEMPLATE (new tabs) | mitigate | extend ONLY inside the existing Jinja autoescape Environment + _CHART_PREFIX whitelist; new chart URIs pass through the same `uri.startswith(_CHART_PREFIX)` boundary in write_report; never add `\|safe` |
| T-04-02 | Information Disclosure | pipeline.py analysis stages | mitigate | analysis-module prints (emoji lines) stay inside the existing `redirect_stdout` capture; console narration uses ASCII-safe labels only |
| T-04-03 | DoS (UX) | network figure wrapper | mitigate | `network_figure` runs under the existing `matplotlib.use("Agg")` in run_pipeline; `_safe_chart` degrades to "" on figure failure — a chart crash never kills the report |
| T-04-04 | Tampering | runtime data leak (DataFrame/DiGraph into report) | mitigate | adapters extract scalars only via defensive .get(); prepared_data DataFrame and networkx DiGraph are never serialized into AnalysisResults (Pattern 3) |
| T-04-SC | Tampering | dependency installs | accept | NO new pip packages in this plan; only code wiring. torch/transformers runtime install arrives in 04-03 and was already audited in Phase 1 (T-01-SC) |
</threat_model>

<verification>
- `python -m pytest tests/test_phase4_alwayson.py tests/test_phase1_smoke.py tests/test_phase2_cli.py tests/test_phase2_report.py tests/test_phase2_pipeline.py -x` — all green
- `python -m ruff check src/chat_analyzer tests` — 0 errors
- Manual spot-check: `chat-analyzer data/sample_chats/whatsapp_sample.txt` from a scratch dir (or BROWSER=__none__) produces `<cwd>/whatsapp_sample_report.html` with Relationship Health and Network tabs
- Smoke contract preserved: `Messages: 27` token still printed once (test_phase1_smoke.py::message_count passes)
</verification>

<success_criteria>
- User-observable: report contains 2 new tabs (Relationship Health, Network) with narrative lead-ins and base64 charts, no [nlp] install required
- Report always written to cwd as `<stem>_report.html` (D-09); no flags (D-08)
- No `logging.basicConfig` at import in relationship_health.py; no `plt.show()` in figure-returning wrappers
- All pre-existing tests green after the location change
</success_criteria>

<output>
Create `.planning/phases/04-nlp-extras-quality-gate/04-01-SUMMARY.md` when done
</output>
