---
phase: 04-nlp-extras-quality-gate
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - tests/test_phase4_nlp.py
  - src/chat_analyzer/analysis/emotion.py
  - src/chat_analyzer/cli/nlp_gate.py
  - src/chat_analyzer/cli/pipeline.py
  - src/chat_analyzer/cli/adapters.py
  - src/chat_analyzer/cli/report_html.py
  - tests/test_phase2_pipeline.py
autonomous: true
requirements: [ANAL-06, ANAL-08, QUAL-02]
must_haves:
  truths:
    - "With NLP available, the report contains Emotion and Conversation Summary tabs with non-uniform emotion scores and a summary text (D-11)"
    - "Emotion classification uses the locked 6-class model bhadresh-savani/distilbert-base-uncased-emotion (D-07c — RESEARCH typo variants ignored)"
    - "The transformers pipeline return is consumed as a flat list of dicts — the `[0]` parse bug is fixed and real scores surface (Pitfall 1, CRITICAL)"
    - "When NLP is unavailable the pipeline silently runs basic analysis — no error, no prompt (D-02/D-06)"
    - "Model name + size are announced before any download/construction that would trigger from_pretrained (D-05, Pitfall 4)"
    - "Terminal shows a real-time progress bar (D-12) that degrades to plain [OK] stage lines when stdout is not a tty (Pitfall 8)"
  artifacts:
    - path: "src/chat_analyzer/cli/nlp_gate.py"
      provides: "silent availability probe + locked model constants"
      contains: "bhadresh-savani/distilbert-base-uncased-emotion"
    - path: "src/chat_analyzer/analysis/emotion.py"
      provides: "fixed 6-class parse + locked model default"
      not_contains: "result = self.pipeline(text[:512])\\[0\\]"
    - path: "src/chat_analyzer/cli/report_html.py"
      provides: "emotion + summary tabs with unavailable-note fallback"
      contains: "id=\"tab-emotion\""
    - path: "tests/test_phase4_nlp.py"
      provides: "faithful-mock e2e proof (D-17)"
      exports: ["test_emotion_summary_with_mocked_nlp"]
  key_links:
    - from: "src/chat_analyzer/cli/pipeline.py"
      to: "chat_analyzer.cli.nlp_gate.nlp_available"
      via: "gate before heavy imports/stages"
      pattern: "nlp_available\\("
    - from: "src/chat_analyzer/cli/pipeline.py"
      to: "chat_analyzer.analysis.emotion.EmotionAnalyzer"
      via: "gated stage inside redirect + announce first"
      pattern: "EmotionAnalyzer\\(\\)"
    - from: "src/chat_analyzer/cli/adapters.py"
      to: "AnalysisResults['emotion']"
      via: "emotion_block from get_emotion_summary"
      pattern: "emotion_block"
---

<objective>
Wire the two heavy NLP insights — Emotion classification (ANAL-06) and Conversation Summarization (ANAL-08) — behind the silent availability gate: fix the CRITICAL emotion parsing bug, lock the 6-class model, add the `nlp_gate` module, run both stages in the pipeline when NLP is available (silently degrade otherwise), extend the report with Emotion + Summary tabs, and upgrade terminal narration to a real-time progress bar (D-12).

Purpose: These are the only `[nlp]`-gated features (D-07/D-07b). The gate must be silent (D-02) — the pipeline always prepares for NLP, and whether the heavy models run depends purely on availability. The parse fix is mandatory: with the current `[0]` indexing, the transformer path always degrades to uniform 1/6 scores and ROADMAP success criterion 1 fails silently.

Output: Emotion + Summary report tabs; working 6-class emotion path; nlp_gate.py availability probe; progress-bar narration. (The interactive download menu + positional hint arrive in 04-03.)

**Plan slice story:** **As a** user with NLP available, **I want to** see Emotion and Conversation Summary tabs in my report automatically, **so that** I get the deep NLP insights without any extra steps.
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-nlp-extras-quality-gate/04-CONTEXT.md  (D-01, D-02, D-05, D-07, D-07b, D-07c, D-11, D-12 locked)
@.planning/phases/04-nlp-extras-quality-gate/04-RESEARCH.md  (Pitfall 1 parse bug + fix, Pitfall 2 model swap, Pitfall 4 announce, Pitfall 7 summarizer ctor, Pitfall 8 non-tty narration; Pattern 1 nlp_gate; Code Examples lines 203-243)
@.planning/phases/04-nlp-extras-quality-gate/04-PATTERNS.md  (emotion.py surgical fixes, nlp_gate.py module shape, pipeline.py extension, reconciliation note 5)
@.planning/codebase/CONVENTIONS.md  (_AVAILABLE try/except ImportError pattern lines 62-99)
@src/chat_analyzer/analysis/emotion.py
@src/chat_analyzer/analysis/summarizer.py
@src/chat_analyzer/cli/pipeline.py
@src/chat_analyzer/cli/adapters.py
@src/chat_analyzer/cli/report_html.py
@tests/test_phase2_pipeline.py

<interfaces>
Locked model (CONTEXT D-07c — RESEARCH.md has typo spellings `bhadresh-savUses`, `bilibili-savati`; use the locked name verbatim):
- MODEL_ID = "bhadresh-savani/distilbert-base-uncased-emotion"  (6 labels: joy/sadness/anger/fear/surprise/love, ~255 MB, ~94% F1)
- Summarizer model (existing): "t5-small" (~231 MB)

Faithful mock shape (D-17, RESEARCH lines 220-229) — patch transformers.pipeline, NEVER the analyzer:
- `[{"label": "joy", "score": 0.87}, {"label": "sadness", "score": 0.03}, {"label": "anger", "score": 0.03}, {"label": "fear", "score": 0.02}, {"label": "surprise", "score": 0.02}, {"label": "love", "score": 0.03}]`

nlp_gate.py target API (RESEARCH Pattern 1 + Pitfall 5 + agent discretion):
- `MODEL_ID: str` = locked emotion model
- `EMOTION_MODEL_SIZE_MB: int = 255`
- `SUMMARY_MODEL_ID: str = "t5-small"`, `SUMMARY_MODEL_SIZE_MB: int = 231`
- `model_cached(model_id: str) -> bool` — check `~/.cache/huggingface/hub/models--{id.replace("/","--")}` (use huggingface_hub.constants.HF_HUB_CACHE if present, else raw path fallback per RESEARCH A3)
- `nlp_available(model_id: str = MODEL_ID) -> bool` — try import transformers+torch, and model_cached(model_id); ALSO honor env override `CHAT_ANALYZER_FORCE_NLP` ("0"→False, "1"→True) for deterministic tests (RESEARCH Pitfall 5: dev machine has transformers but no cached model — tests must force branches)
- Must never raise (import failures return False)
- The guarded runtime installer `install_nlp(cpu_only: bool) -> None` is consumed by 04-03 (menu) — design the signature now, implement in 04-03; in THIS plan only the availability probe + constants + env override ship

From src/chat_analyzer/analysis/emotion.py (VERIFIED):
- Line 39: `def __init__(self, model_name: str = "j-hartmann/emotion-english-distilroberta-base"):` — 7-class, wrong for ANAL-06
- Lines 103-116: `result = self.pipeline(text[:512])[0]` then `{item['label']: item['score'] for item in result}` — BROKEN: top_k=None returns a flat list of dicts; `[0]` is one dict; iterating dict keys → TypeError → caught → neutral fallback. The transformer path ALWAYS returns uniform 1/6 scores.
- Keep: module singletons `_emotion_analyzer`/`_emotion_model_loaded` (lines 29-30), lazy `from transformers import pipeline` inside `_initialize_model` (60-79), `_get_neutral_emotions` (125-127), `get_emotion_summary` (215-271) returning `emotion_distribution`/`average_emotion_scores`/`emotion_intensity`/`by_sender`.

From src/chat_analyzer/analysis/summarizer.py (VERIFIED):
- `ConversationSummarizer(model_name="t5-small")` downloads T5 in __init__ (Pitfall 7 — construct ONLY after the gate + announce, inside try/except that degrades to "summary unavailable")
- `summarize_conversation(df, max_messages=100)` returns dict with `summary` (text), `total_messages`, `messages_summarized`, `model_used`

Current charts set after plan 04-01: {timeline, activity, participants, sentiment, health, network} (6 keys). This plan adds "emotion" (chart). Summary tab is TEXT ONLY — no chart key.
</interfaces>
</context>

<tasks>

<task type="tdd" tdd="true">
  <name>Task 1: Failing e2e test with faithful mocked NLP</name>
  <files>tests/test_phase4_nlp.py</files>
  <read_first>tests/test_phase2_pipeline.py (in-process run_pipeline + _console pattern), src/chat_analyzer/analysis/emotion.py (current broken parse — the mock must drive the REAL method), .planning/phases/04-nlp-extras-quality-gate/04-RESEARCH.md (Code Examples lines 220-230 faithful mock)</read_first>
  <behavior>
    - Test A (gate ON): patch chat_analyzer.cli.nlp_gate.nlp_available → True AND patch chat_analyzer.analysis.emotion.transformers.pipeline → faithful list-of-dicts mock; run_pipeline(whatsapp sample) → results["emotion"]["distribution"] exists and is NOT uniform (assert at least two distinct counts / dominant differs from others); results["summary"]["text"] is a non-empty string; results["charts"] contains "emotion" with base64 prefix.
    - Test B (gate OFF): patch nlp_gate.nlp_available → False; run_pipeline → results["emotion"] is None, results["summary"] is None, run completes without error and basic sections (stats/participants/sentiment) still present.
    - Test C: run_pipeline → write_report with results → HTML contains id="tab-emotion" and id="tab-summary".
  </action>
  Create tests/test_phase4_nlp.py (pytest style). Reuse the `_console()` pattern (Console(file=io.StringIO(), force_terminal=False)). For the transformers mock: `unittest.mock.patch("chat_analyzer.analysis.emotion.transformers.pipeline", return_value=<faithful list-of-dicts>)` — patch the module attribute where emotion.py imports it (D-17: mock the heavy callable, exercise the real EmotionAnalyzer/analyze pipeline). The summarizer path: ConversationSummarizer.__init__ calls T5Tokenizer/T5ForConditionalGeneration.from_pretrained + pipeline — mock at `chat_analyzer.analysis.summarizer.transformers.pipeline` and the from_pretrained classes (`T5Tokenizer.from_pretrained`, `T5ForConditionalGeneration.from_pretrained`) with a fake summarizer whose call returns `[{"summary_text": "A test summary."}]`, OR simpler: mock `ConversationSummarizer.summarize_conversation` on the real class to return a fixed dict — but ONLY if construction is mocked too; the cleanest is to mock the three transformers symbols. The pipeline gate: `unittest.mock.patch("chat_analyzer.cli.pipeline.nlp_gate.nlp_available", return_value=True/False)` (or patch the nlp_gate module attribute). Do NOT implement production code — the tests must fail now (ModuleNotFoundError for chat_analyzer.cli.nlp_gate). Run and confirm RED.
  </action>
  <acceptance_criteria>
    - tests/test_phase4_nlp.py exists with tests A/B/C
    - `python -m pytest tests/test_phase4_nlp.py -x` fails (ModuleNotFoundError: chat_analyzer.cli.nlp_gate) — RED is the success condition
    - No production source file modified
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_phase4_nlp.py -x; expect FAIL (RED)</automated>
  </verify>
  <done>Failing mocked-NLP e2e tests prove the gated slice is not yet delivered</done>
</task>

<task type="auto">
  <name>Task 2: Thinnest slice — emotion fixes, nlp_gate, pipeline stages, adapters, report tabs</name>
  <files>src/chat_analyzer/analysis/emotion.py, src/chat_analyzer/cli/nlp_gate.py, src/chat_analyzer/cli/pipeline.py, src/chat_analyzer/cli/adapters.py, src/chat_analyzer/cli/report_html.py</files>
  <read_first>src/chat_analyzer/analysis/emotion.py (lines 39-127: __init__ default, analyze_single_message, _get_neutral_emotions), src/chat_analyzer/cli/pipeline.py (current run_pipeline incl. plan 04-01 health/network stages), src/chat_analyzer/cli/adapters.py (current adapt signature + health/network blocks), src/chat_analyzer/cli/report_html.py (current TEMPLATE + write_report render call), .planning/phases/04-nlp-extras-quality-gate/04-RESEARCH.md (Code Examples lines 204-219 exact surgical fix)</read_first>
  <action>
    Five files, one gated vertical slice. Per D-02/D-05/D-07c/D-11:

    1. emotion.py — THREE surgical edits, no rewrite (Pitfalls 1, 2, 3):
       (a) Line 39: change default to `model_name: str = "bhadresh-savani/distilbert-base-uncased-emotion"` (D-07c — locked; do NOT use RESEARCH typo spellings).
       (b) Lines 103-116 analyze_single_message transformer branch: drop `[0]` — `res = self.pipeline(text[:512])`; `scores = {r["label"]: float(r["score"]) for r in res}`; then `for e in self.emotions: scores.setdefault(e, 0.0)`; `return scores`. Keep the empty/media/short-message guards and the except→_get_neutral_emotions degrade.
       (c) Scope the module-top `warnings.filterwarnings('ignore')` (line 26) — move it inside the try block of `_initialize_model` (or leave module-level if it must cover the transformers import; the minimum: do not remove it — Pitfall 3 allows leaving it; only relationship_health's basicConfig is mandatory).
       (d) Add a thin figure-returning helper `emotion_figure(summary: dict) -> "matplotlib.figure.Figure"`: import matplotlib.pyplot inside, build a bar chart from `summary.get("average_emotion_scores")` (x = 6 emotion labels, y = scores), ax.set_title("Emotion Scores"), `return fig` — NO plt.show() (Pitfall 6). Do NOT touch plot_emotion_analysis (legacy, plt.show()).
       (e) Keep module singletons and lazy transformers import.
    2. nlp_gate.py (NEW) — thin module per the <interfaces> contract: `MODEL_ID`, `EMOTION_MODEL_SIZE_MB`, `SUMMARY_MODEL_ID`, `SUMMARY_MODEL_SIZE_MB` constants; `model_cached(model_id)` (huggingface_hub.constants.HF_HUB_CACHE with raw `~/.cache/huggingface/hub` fallback); `nlp_available(model_id=MODEL_ID)` — checks env override `CHAT_ANALYZER_FORCE_NLP` first ("0"→False, "1"→True, absent→probe), then try-import transformers+torch (except ImportError → False), then model_cached(model_id). Never raises. Follow pipeline.py's module shape (future-annotations import, stdlib first, module logger).
    3. pipeline.py — inside run_pipeline "Computing insights":
       (a) Gate BEFORE heavy imports: `from chat_analyzer.cli import nlp_gate; nlp_on = nlp_gate.nlp_available(nlp_gate.MODEL_ID)`.
       (b) When nlp_on: (i) console.print the announce INSIDE the stage_status context but OUTSIDE the redirect_stdout block so it reaches the user (Pitfall 4/8): announce emotion model with `f"Emotion model: {nlp_gate.MODEL_ID} (~{nlp_gate.EMOTION_MODEL_SIZE_MB} MB)"` — ASCII, no emoji; (ii) inside a NEW redirect_stdout block (or reusing the existing one — restructure so the announce is outside the capture): construct `from chat_analyzer.analysis.emotion import EmotionAnalyzer, emotion_figure; emo_analyzer = EmotionAnalyzer(); df_emo = emo_analyzer.analyze_emotions(df); emotion_summary = emo_analyzer.get_emotion_summary(df_emo)` wrapped in try/except Exception → emotion_summary = None on failure (degrade, never crash); (iii) summarization: announce `f"Summary model: {nlp_gate.SUMMARY_MODEL_ID} (~{nlp_gate.SUMMARY_MODEL_SIZE_MB} MB)"` then inside try/except: `from chat_analyzer.analysis.summarizer import ConversationSummarizer; conv_summary = ConversationSummarizer().summarize_conversation(df)` (Pitfall 7 — construct only now, degrade to a dict like `{"summary": "Summary unavailable.", "messages_summarized": 0}` on failure); (iv) charts dict gains `"emotion": _safe_chart(emotion_figure(emotion_summary))` when emotion_summary is not None.
       (c) When not nlp_on: emotion_summary = None, conv_summary = None — silent basic (D-06; the hint line is main.py's job in 04-03).
       (d) Pass `emotion=emotion_summary, summary=conv_summary` to adapt(...) (None when unavailable). Keep the `_sentiment.TRANSFORMERS_AVAILABLE = False` pin and the smoke token.
    4. adapters.py — extend with `emotion_block` and `summary_block` (params already in the signature from 04-01 with None defaults):
       emotion_block keys (from get_emotion_summary result, defensive .get()): `distribution` ← `{str(k): int(v) for k, v in (emotion.get("emotion_distribution") or {}).items()}`, `average_scores` ← `emotion.get("average_emotion_scores") or {}`, `dominant` ← argmax of distribution (None when empty), `total_messages` ← emotion.get("total_messages"). summary_block keys: `text` ← summary.get("summary"), `messages_summarized` ← summary.get("messages_summarized"), `model_used` ← summary.get("model_used"). Guard: `"emotion": emotion_block if emotion is not None else None`, `"summary": summary_block if summary is not None else None`. build_insights: cap 7→11 already done in 04-01; append an emotion lead-in (dominant emotion, only when distribution present) and a summary lead-in (first ~80 chars of text, only when text present); never "None".
    5. report_html.py — TEMPLATE: add nav buttons `data-tab="emotion"` (Emotion) and `data-tab="summary"` (Summary); panels id="tab-emotion" (lead insights[7], `{% if charts.emotion %}` chart img, table of distribution + dominant) and id="tab-summary" (lead insights[8], summary text paragraph, messages_summarized/model_used metadata). Wrap each panel body in `{% if emotion %}` ... `{% else %}` a friendly note: "Install the NLP extra for emotion analysis: pip install chat-analyzer-pro[nlp]" (and same for summary) so basic runs show an actionable hint inside the tab (D-06 spirit). render(): pass `emotion=results.get("emotion")`, `summary=results.get("summary")` (defensive).
  </action>
  <acceptance_criteria>
    - emotion.py default `model_name: str = "bhadresh-savani/distilbert-base-uncased-emotion"` and NO `self.pipeline(text[:512])[0]` (grep for `\[0\]` in the transformer branch)
    - nlp_gate.py contains MODEL_ID with the locked name, model_cached, nlp_available, CHAT_ANALYZER_FORCE_NLP handling, and no raise paths
    - pipeline.py contains `nlp_on = nlp_gate.nlp_available(` and passes emotion= / summary= into adapt(...)
    - report_html.py contains id="tab-emotion" and id="tab-summary" and the pip-install unavailable note
    - `python -m pytest tests/test_phase4_nlp.py -x` passes (GREEN)
    - `python -m pytest tests/test_phase1_smoke.py tests/test_phase2_pipeline.py -x` still passes (VADER pin + smoke contract intact; gate defaults to unavailable on dev machine since the model is not cached)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_phase4_nlp.py tests/test_phase1_smoke.py tests/test_phase2_pipeline.py -x</automated>
  </verify>
  <done>Emotion + Summary render from real modules when the gate is ON; silent basic path when OFF</done>
</task>

<task type="auto">
  <name>Task 3: D-12 real-time progress bar + deterministic charts-set pin</name>
  <files>src/chat_analyzer/cli/pipeline.py, tests/test_phase2_pipeline.py</files>
  <read_first>src/chat_analyzer/cli/pipeline.py (stage_status at 50-61, run_pipeline stage calls), tests/test_phase2_cli.py (test_stage_narration_and_order asserts stage lines "Parsing chat"/"Computing insights"/"Writing report" on non-tty), tests/test_phase2_pipeline.py (charts-set assertion)</read_first>
  <action>
    Per D-12 (real-time progress bar / loading screen) + Pitfall 8 (non-tty narration) + reconciliation note #5.

    1. pipeline.py — upgrade stage narration to a real-time progress experience WITHOUT breaking the non-tty contract:
       (a) Keep `stage_status(console, label)` behavior for non-tty (plain `[OK] <label>...` lines — tests rely on these exact labels appearing).
       (b) When `console.is_terminal`, wrap the whole run_pipeline flow in a `rich.progress.Progress` instance: create `progress = Progress(..., transient=False)` with a single determinate task (`total=6` in plan 04-02: Parsing chat, Computing insights, Analyzing emotions, Summarizing conversation, Writing report is outside pipeline... use the stages pipeline actually runs) and `progress.update(task_id, advance=1, description=label)` at each stage boundary, so the user sees a live loading bar. Replace each `with stage_status(console, label):` in the run flow with a helper `stage(console, progress, task, label)` that advances and narrates. Non-tty path unchanged (stage_status fallback). Stage labels MUST remain exactly "Parsing chat", "Computing insights", plus new "Analyzing emotions" and "Summarizing conversation" for the gated stages (04-02's stages) so test_stage_narration_and_order in test_phase2_cli.py keeps passing for the existing three.
       (c) The emotion/summary announce prints (from Task 2) stay inside the stage narration (console.print within the stage context) — they must reach piped output too (Pitfall 8).
    2. tests/test_phase2_pipeline.py — test_whatsapp_e2e: pin the gate deterministically. Patch `chat_analyzer.cli.pipeline.nlp_gate.nlp_available` → False (import the module and monkeypatch/setattr the attribute in the test) and assert the charts set == `{"timeline", "activity", "participants", "sentiment", "health", "network"}` (6 keys — emotion is gated off). Add a comment that test_phase4_nlp.py covers the gate-ON 7-key set. This removes the environment flake where a cached model on the dev machine would change the set (Pitfall 5).
    3. Verify the progress upgrade preserves: smoke token "Messages: 27" (printed by main.py, untouched), stage narration lines on non-tty, `matplotlib.use("Agg")` first in run_pipeline.
  </action>
  <acceptance_criteria>
    - pipeline.py contains a rich Progress usage (`Progress(` / `progress.`) for the terminal path and unchanged `[OK] <label>` fallback for non-tty
    - test_phase2_pipeline.py::test_whatsapp_e2e pins nlp availability False and asserts the exact 6-key set
    - `python -m pytest tests/test_phase2_pipeline.py tests/test_phase2_cli.py -x` passes
    - `python -m ruff check src/chat_analyzer tests` passes (0 errors)
  </acceptance_criteria>
  <verify>
    <automated>python -m pytest tests/test_phase2_pipeline.py tests/test_phase2_cli.py -x && python -m ruff check src/chat_analyzer tests</automated>
  </verify>
  <done>Terminal shows a live progress bar on tty and deterministic stage lines off-tty; charts-set assertions are environment-stable</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ~/.cache/huggingface (filesystem) → nlp_gate probe | availability probe reads the local model cache |
| transformers/torch import → pipeline | heavy import executed only after the gate passes |
| chat content → T5 summarizer → report | untrusted message text is summarized by a model and rendered into HTML |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-05 | Tampering | pipeline NLP stages | mitigate | heavy imports/stages run ONLY after `nlp_gate.nlp_available()` returns True; every heavy stage is wrapped in try/except → degrade to None, never crash the run (Pitfall 7) |
| T-04-06 | DoS (UX) | model construction/download | mitigate | announce model name + size via console.print BEFORE EmotionAnalyzer()/ConversationSummarizer() construction (Pitfall 4); the announce sits outside the redirect_stdout capture so it reaches piped output too (Pitfall 8) |
| T-04-07 | Tampering | chat content → summary text → HTML | mitigate | summary text and emotion labels are rendered ONLY through the existing Jinja autoescape Environment; no `\|safe` on model output (same boundary as T-04-01) |
| T-04-08 | Tampering | emotion parse regression | mitigate | surgical fix drops `[0]`; faithful list-of-dicts mock in test_phase4_nlp.py asserts non-uniform scores so the bug cannot silently return (Pitfall 1 regression trap) |
| T-04-09 | Spoofing | prompt injection via chat content into summarizer | accept | local CLI, output rendered autoescaped; no code execution from model output; low-value target (no PII exfiltration surface beyond the local report) |
| T-04-SC | Tampering | dependency installs | accept | NO new pip packages; torch/transformers already declared in `[nlp]` extra (pyproject.toml:27) and audited in Phase 1 (T-01-SC). Runtime re-install arrives in 04-03 with the subprocess discipline |
</threat_model>

<verification>
- `python -m pytest tests/test_phase4_nlp.py tests/test_phase1_smoke.py tests/test_phase2_pipeline.py tests/test_phase2_cli.py -x` — all green
- `python -m ruff check src/chat_analyzer tests` — 0 errors
- Manual: with `CHAT_ANALYZER_FORCE_NLP=1` on a machine with transformers, run pipeline on the sample → Emotion + Summary tabs populated; with `=0` → tabs show the pip-install note, run completes
- No "🚀"/"Initializing Sentiment" in captured stdout (analysis-stage prints stay inside redirect — test_no_console_pollution)
</verification>

<success_criteria>
- 6-class emotion classification returns real (non-uniform) scores through the real analyze_emotions path when the gate is ON
- Report contains Emotion + Summary tabs; basic runs show actionable unavailable-notes, never errors
- Gate is silent: no prompts, no hints inside the pipeline (menu/hint are main.py's job in 04-03)
- Progress bar narration on tty; identical stage-line narration on non-tty; smoke contract intact
</success_criteria>

<output>
Create `.planning/phases/04-nlp-extras-quality-gate/04-02-SUMMARY.md` when done
</output>
