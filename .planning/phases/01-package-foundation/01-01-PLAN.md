---
phase: 01-package-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - "src/chat_analyzer/ (moved from src/: analysis/, parser/, ingest/, utils/, reporting/, _init_.py)"
  - "src/chat_analyzer/__init__.py (rewritten — light marker)"
  - "src/chat_analyzer/analysis/__init__.py (cleaned re-exports)"
  - "src/chat_analyzer/parser/__init__.py (fixed parse_telegram_json -> parse_telegram_chat)"
  - "src/chat_analyzer/reporting/__init__.py (docstring src.* fix, line 9)"
  - "src/chat_analyzer/ingest/__init__.py (new marker)"
  - "src/chat_analyzer/analysis/relationship_health.py (import-site fix, line 800)"
  - "src/chat_analyzer/analysis/emotion.py (docstring fix, line 15)"
  - "src/chat_analyzer/utils/visualization.py (docstring fix, line 685)"
  - "src/chat_analyzer/analysis/summarizer.py (lazy transformers import, line 12)"
  - "pyproject.toml (new)"
  - ".planning/PROJECT.md (Python floor fix per D-09)"
  - "deleted: app/, deployment/, .streamlit/, apt.txt, packages.txt, requirements.txt"
autonomous: false
requirements: [PKG-01, PKG-02, PKG-03, PKG-04]
user_setup: []

must_haves:
  truths:
    - "src/chat_analyzer/ is a real importable package: valid __init__.py markers everywhere, zero _init_.py files remain"
    - "No `from src.*` / `import src.*` statements remain anywhere in the package"
    - "chat_analyzer.analysis.summarizer imports successfully WITHOUT transformers installed (lazy gate)"
    - "pyproject.toml declares requires-python >=3.11, base dependencies without torch/transformers/streamlit/plotly, and an [nlp] optional extra containing torch + transformers"
    - "Web-app artifacts (app/, deployment/, .streamlit/, apt.txt, packages.txt) are deleted from the repo"
    - "Importing chat_analyzer does NOT eagerly load heavy modules (pandas, matplotlib, reportlab, seaborn, networkx)"
  artifacts:
    - path: "src/chat_analyzer/__init__.py"
      provides: "Light package marker — docstring + __version__ + __all__; NO eager `from . import ...` subpackage imports"
      contains: "__version__"
    - path: "src/chat_analyzer/analysis/__init__.py"
      provides: "Cleaned re-exports — exports plot_relationship_health_dashboard_enhanced; no analyze_sentiment/perform_eda/classify_emotions"
      contains: "plot_relationship_health_dashboard_enhanced"
    - path: "src/chat_analyzer/parser/__init__.py"
      provides: "Fixed re-export — parse_telegram_chat (not parse_telegram_json)"
      contains: "parse_telegram_chat"
    - path: "src/chat_analyzer/ingest/__init__.py"
      provides: "New package marker for ingest/ (had none)"
      contains: "process_uploaded_file"
    - path: "pyproject.toml"
      provides: "Build backend, deps, [nlp]/[dev] extras, console script"
      contains: "requires-python = \">=3.11\""
  key_links:
    - from: "pyproject.toml [project.scripts]"
      to: "chat_analyzer.cli:app"
      via: "console script declaration (D-01; target created in plan 02)"
      pattern: "chat-analyzer = \"chat_analyzer.cli:app\""
    - from: "src/chat_analyzer/analysis/relationship_health.py:800"
      to: "chat_analyzer.utils.visualization"
      via: "in-function lazy import (was src.utils.visualization)"
      pattern: "from chat_analyzer.utils.visualization import ChatVisualizer"
    - from: "src/chat_analyzer/analysis/summarizer.py"
      to: "transformers"
      via: "import moved inside __init__ try block (no module-top import)"
      pattern: "from transformers import"
---

<objective>
Package surgery: repackage `src/` into the `src/chat_analyzer/` import package, create `pyproject.toml`, delete the web app, and fix the Python floor. This is the prerequisite slice for everything else — after this plan, the project is structurally a proper src-layout package, importable as `chat_analyzer.*`, with a valid pyproject ready for `pip install` (the actual install + command verification happens in plan 02).

Purpose: Today the repo is unshippable — package markers are misnamed `_init_.py` (PEP 420 namespace tolerance only), `src` cannot ship as a top-level package name, and no pyproject exists. Every later phase (parser hardening, pipeline, renderers) needs `pip install -e .` + working imports first.
Output: `src/chat_analyzer/` with valid markers and fixed imports, `pyproject.toml`, deleted web-app artifacts, corrected PROJECT.md floor, and a human-approved package-legitimacy gate for the new install-time dependencies.
</objective>

<execution_context>
@C:/Users/KIIT0001/.config/opencode/get-shit-done/workflows/execute-plan.md
@C:/Users/KIIT0001/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-package-foundation/01-CONTEXT.md
@.planning/phases/01-package-foundation/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/codebase/CONVENTIONS.md
@.planning/codebase/CONCERNS.md
@AGENTS.md

<interfaces>
<!-- Verified facts the executor needs — no re-exploration required. -->

Source-of-truth for the package move and marker cleanup (PATTERNS.md):
- `src/analysis/_init_.py` broken re-exports: `plot_relationship_health_dashboard` (actual symbol: `plot_relationship_health_dashboard_enhanced`, defined relationship_health.py:784); try/except block importing `analyze_sentiment`, `perform_eda`, `classify_emotions` (none exist — the except swallows today; the rename activates them).
- `src/parser/_init_.py:14` broken: `parse_telegram_json` (actual: `parse_telegram_chat`, telegram_parser.py:6). `parse_whatsapp_chat` is valid.
- `src/utils/_init_.py` re-exports (ChatVisualizer, preprocess_text, clean_messages, extract_emojis) — ALL valid, keep unchanged.
- `src/reporting/_init_.py` re-exports (generate_chat_analysis_pdf, ChatAnalysisPDFGenerator) — ALL valid, keep unchanged.
- `src/_init_.py` content valid but the four eager `from . import parser/analysis/reporting/utils` lines MUST be removed — they pull pandas/matplotlib/reportlab at `import chat_analyzer`, breaking the instant `--help` requirement (research Anti-Pattern 2, Pitfall 8).

Verified `from src.*` sites (exactly 3 in module code+docstrings, plus 3 docstring-only refs in markers — ALL six must be fixed):
- `src/analysis/relationship_health.py:800` — real lazy in-function import: `from src.utils.visualization import ChatVisualizer`
- `src/analysis/emotion.py:15` — module docstring usage example
- `src/utils/visualization.py:685` — `if __name__ == "__main__":` demo docstring
- `src/analysis/_init_.py:11-13`, `src/parser/_init_.py:9-10`, `src/reporting/_init_.py:9` — marker docstring usage lines (ALL updated when markers are rewritten; reporting:9 `from src.reporting.pdf_report import generate_chat_analysis_pdf` matches the zero-match grep gate, so it is a REQUIRED fix, not optional)

Verified dependency reality (grep over src/, not requirements.txt assumptions):
- Top-level third-party imports: requests (telegram_parser.py:4 — required for module import), reportlab (pdf_report.py:25-31 — required for `import chat_analyzer.reporting`), networkx (network_graph.py:18)
- Lazy/gated imports: wordcloud (visualization.py:189 try/except), textblob (sentiment.py:18 try/except — optional, degrades), PIL (ingestion.py:43 try/except DEPENDENCIES), transformers (sentiment.py:25 try/except, emotion.py:61 in-method try/except, summarizer.py:12 TOP-LEVEL — the fix target)
- NOT imported anywhere in src/ (verified zero matches): nltk, scipy, tqdm, emoji, regex, python-dateutil, pytz, streamlit, plotly, python-dotenv — do NOT carry into pyproject
- pandas (hard dep of pandas) brings python-dateutil transitively — no explicit declaration needed

Verified symbol names in ingestion.py for the new marker:
- `def process_uploaded_file(uploaded_file: Any) -> Tuple[List[Dict[str, Any]], List[Dict[str, Any]]]` (line 399)
- `def get_dependency_status() -> Dict[str, bool]` (line 96)
- `def get_supported_formats() -> Dict[str, List[str]]` (line 627)

Summarizer fix target (verified): T5/pipeline names are referenced ONLY inside `__init__` (lines 52-55). Moving the import into the existing try block is safe.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure src/ into src/chat_analyzer/ with valid package markers and fix all import sites</name>
  <files>
    src/chat_analyzer/ (all moved modules), src/chat_analyzer/__init__.py, src/chat_analyzer/analysis/__init__.py,
    src/chat_analyzer/parser/__init__.py, src/chat_analyzer/ingest/__init__.py (new),
    src/chat_analyzer/reporting/__init__.py (docstring src.* fix, line 9),
    src/chat_analyzer/analysis/relationship_health.py, src/chat_analyzer/analysis/emotion.py,
    src/chat_analyzer/utils/visualization.py, src/chat_analyzer/analysis/summarizer.py
  </files>
  <read_first>
    .planning/phases/01-package-foundation/01-PATTERNS.md (marker verdict table + import-site fixes — the source of truth for what is broken vs valid)
    src/analysis/_init_.py, src/parser/_init_.py, src/utils/_init_.py, src/reporting/_init_.py, src/_init_.py (current marker contents to rewrite)
    src/analysis/summarizer.py (lines 1-70 — the import to make lazy)
    src/analysis/relationship_health.py (lines 795-805 — import site), src/analysis/emotion.py (lines 1-20 — docstring), src/utils/visualization.py (lines 678-690 — docstring)
    .planning/codebase/CONVENTIONS.md (marker shape: docstring + re-exports + __all__; double quotes in new code)
  </read_first>
  <action>
    Use git mv throughout (preserves history). Create `src/chat_analyzer/` first (New-Item -ItemType Directory), then:
    git mv src/analysis, src/parser, src/ingest, src/utils, src/reporting into src/chat_analyzer/; git mv src/_init_.py src/chat_analyzer/_init_.py.
    Rename every `_init_.py` → `__init__.py` via git mv (5 files: the package root + analysis/, parser/, utils/, reporting/).

    Marker rewrites (use the Write tool; keep the Google-style module docstrings — do NOT strip them):
    1. src/chat_analyzer/__init__.py — keep docstring, __version__ = "0.1.0" (MUST match the pyproject.toml version in Task 2 — single version source of truth so package attribute and installer metadata never drift), __author__, __project__, __all__ listing subpackage names. REMOVE the four lines `from . import parser / analysis / reporting / utils` — eager subpackage imports pull pandas/matplotlib/reportlab and break instant `--help` (research Anti-Pattern 2). `from chat_analyzer import X` still works for consumers via normal submodule import machinery.
    2. src/chat_analyzer/analysis/__init__.py — delete the try/except block importing analyze_sentiment, perform_eda, classify_emotions (none of these symbols exist). Change `plot_relationship_health_dashboard` → `plot_relationship_health_dashboard_enhanced` in both the import statement and __all__. Update the docstring usage lines from `src.analysis...` to `chat_analyzer.analysis...`.
    3. src/chat_analyzer/parser/__init__.py — change `parse_telegram_json` → `parse_telegram_chat` in the import and __all__. Update docstring usage line to `chat_analyzer.parser...`.
    4. src/chat_analyzer/utils/__init__.py — keep content unchanged (all re-exports verified valid). The docstring usage line mentioning `src.` may be updated to `chat_analyzer.` (optional, cosmetic). src/chat_analyzer/reporting/__init__.py — REQUIRED docstring fix: line 9 `from src.reporting.pdf_report import generate_chat_analysis_pdf` → `from chat_analyzer.reporting.pdf_report import generate_chat_analysis_pdf`. It must be updated (NOT optional) because the line matches the `from src\.|import src\.` grep gate in verify step 3, which demands exactly 0 matches. Re-exports and all other content stay unchanged.
    5. Create src/chat_analyzer/ingest/__init__.py (NEW — ingest had no marker): module docstring + `from .ingestion import process_uploaded_file, get_dependency_status, get_supported_formats` + __all__ with those three names. All three symbols verified at ingestion.py:399/96/627.

    Import-site fixes (exactly these, no other content changes to moved modules — reuse, not rewrite):
    1. src/chat_analyzer/analysis/relationship_health.py:800 — `from src.utils.visualization import ChatVisualizer` → `from chat_analyzer.utils.visualization import ChatVisualizer`. Keep the enclosing try/except-lazy structure and the `use_viz_module` flag exactly as-is; only the import path string changes.
    2. src/chat_analyzer/analysis/emotion.py:15 — docstring `from src.analysis.emotion import EmotionAnalyzer` → `from chat_analyzer.analysis.emotion import EmotionAnalyzer`. No code change.
    3. src/chat_analyzer/utils/visualization.py:685 — docstring `from src.utils.visualization import ChatVisualizer, quick_dashboard` → `from chat_analyzer.utils.visualization import ChatVisualizer, quick_dashboard`. No code change.

    Lazy transformers import in summarizer.py (planner discretion, explicitly flagged in context — REQUIRED for QUAL-01 "all modules import" on a base install):
    - Delete the module-top line 12: `from transformers import pipeline, T5Tokenizer, T5ForConditionalGeneration`.
    - Add the identical import as the FIRST line inside the existing `try:` block in `ConversationSummarizer.__init__` (currently lines 51-62, before `self.tokenizer = T5Tokenizer.from_pretrained(model_name)`).
    - Verified: these three names are referenced ONLY inside __init__ (lines 52-55) — no other method uses them, so the in-init import cannot shadow anything.
    - Keep the existing `except Exception as e: print(...); raise` behavior unchanged.

    Do NOT touch: tests/ (self-contained mocks, Phase 4 concern), data/, notebooks/, README.md, changelog.md.
  </action>
  <verify>
    <automated>
      # 1. All moved files compile
      python -m compileall -q src/chat_analyzer

      # 2. No _init_.py remains
      Get-ChildItem src/chat_analyzer -Recurse -Filter *_init_.py | Measure-Object | Select-Object -ExpandProperty Count

      # 3. No from src.* / import src.* remains — covers module code AND marker docstrings, reporting/_init_.py:9 included (grep gate — use Select-String, rg is not installed)
      Get-ChildItem src/chat_analyzer -Recurse -Filter *.py | Select-String -Pattern "from src\.|import src\." | Measure-Object | Select-Object -ExpandProperty Count

      # 4. Lazy-import proof + import smoke (PYTHONPATH=src; transformers must NOT be needed)
      $env:PYTHONPATH="src"; python -c "import chat_analyzer; import chat_analyzer.analysis.summarizer; import chat_analyzer.parser.whatsapp_parser; import chat_analyzer.parser.telegram_parser; import chat_analyzer.ingest.ingestion; import chat_analyzer.utils.visualization; import chat_analyzer.analysis.emotion; print('IMPORTS-OK')"

      # 5. Light-package proof: importing chat_analyzer must not load heavy modules
      $env:PYTHONPATH="src"; python -c "import sys, chat_analyzer; heavy=[m for m in ('pandas','matplotlib','reportlab','seaborn','networkx') if m in sys.modules]; assert not heavy, heavy; print('LIGHT-OK')"
    </automated>
  </verify>
  <acceptance_criteria>
    - `compileall -q src/chat_analyzer` exits 0 (no syntax errors introduced by the move)
    - Exactly 0 files named `*_init_.py` under src/chat_analyzer/
    - Exactly 0 matches for `from src\.|import src\.` across all src/chat_analyzer/**/*.py
    - PYTHONPATH import check prints IMPORTS-OK — proves summarizer imports without transformers (pre-fix this fails with ModuleNotFoundError: transformers)
    - PYTHONPATH light check prints LIGHT-OK — proves `import chat_analyzer` loads no pandas/matplotlib/reportlab/seaborn/networkx
    - src/chat_analyzer/analysis/__init__.py contains `plot_relationship_health_dashboard_enhanced` and does NOT contain `analyze_sentiment` / `perform_eda` / `classify_emotions`
    - src/chat_analyzer/parser/__init__.py contains `parse_telegram_chat` and does NOT contain `parse_telegram_json`
    - src/chat_analyzer/reporting/__init__.py:9 contains `chat_analyzer.reporting` and does NOT contain `src.reporting` (docstring fixed — the zero-match grep gate in verify #3 spans every marker docstring site, reporting included)
    - src/chat_analyzer/ingest/__init__.py exists and contains `process_uploaded_file`
    - src/chat_analyzer/analysis/summarizer.py has no top-level `from transformers import` (the only occurrence is inside the `__init__` try block)
  </acceptance_criteria>
  <done>
    The package is importable as chat_analyzer.* via PYTHONPATH=src with valid markers, zero stale src.* imports, lazy summarizer, and a light top-level marker. All 4 content-edit modules (relationship_health, emotion, visualization, summarizer) retain all other logic byte-identical to pre-move.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create pyproject.toml (hatchling, >=3.11, [nlp] extra, console script), delete web-app artifacts, fix PROJECT.md floor</name>
  <files>
    pyproject.toml (new), deleted: app/, deployment/, .streamlit/, apt.txt, packages.txt, requirements.txt,
    .planning/PROJECT.md
  </files>
  <read_first>
    requirements.txt (current dep list — source of truth for names, then pruned per verified imports)
    .planning/research/STACK.md (verified versions: typer 0.27.0, rich 15.0.0, plotext 5.3.2, hatchling 1.31.0; transformers pin <6)
    .planning/phases/01-package-foundation/01-CONTEXT.md (decisions D-01, D-05, D-06, D-09)
    .planning/codebase/CONCERNS.md (duplicated-manifests anti-pattern, lines 42-45)
    .planning/PROJECT.md (line 60 — the stale floor to fix)
  </read_first>
  <action>
    Create pyproject.toml with the Write tool — hatchling build backend (PEP 517/621), src-layout. Exact required structure:

    [build-system] requires = ["hatchling>=1.31.0"], build-backend = "hatchling.build"

    [project]
    name = "chat-analyzer-pro"
    version = "0.1.0"
    description = "Analyze WhatsApp and Telegram chat exports from the terminal"
    requires-python = ">=3.11"   # D-09 — NOT research's >=3.9 example; the verified floor

    [project] dependencies — the VERIFIED-IMPORT list (every name below is confirmed imported by src/ modules; do not blind-copy requirements.txt):
    pandas>=2.0.0, numpy>=1.24.0, matplotlib>=3.7.0, seaborn>=0.12.0, vaderSentiment>=3.3.2,
    wordcloud>=1.9.0, networkx>=3.1, requests>=2.31.0, reportlab>=4.0.0, Pillow>=10.0.0,
    typer>=0.27, rich>=13, plotext>=5.3

    [project.optional-dependencies]
    nlp = ["torch>=2.0", "transformers>=4.30,<6"]     # PKG-02: heavy deps ONLY here. transformers<6 pin is critical (5.x breaks the 4.x-era core code)
    dev = ["pytest>=7.4", "pytest-cov>=4.1", "ruff>=0.16.1"]

    [project.scripts]
    chat-analyzer = "chat_analyzer.cli:app"           # D-01 exact target — cli package created in plan 02; entry-point resolution is lazy so install succeeds now, invocation verified then

    [tool.hatch.build.targets.wheel]
    packages = ["src/chat_analyzer"]

    DELIBERATELY EXCLUDED from pyproject (documented rationale — verified zero imports in src/): streamlit, plotly, python-dotenv (web-app deps, D-06), nltk (never imported), scipy, tqdm, emoji, regex, python-dateutil, pytz (python-dateutil arrives transitively as a pandas hard dep; the rest are unused). textblob excluded deliberately — sentiment.py:18 try/except degrades to VADER-only when absent (existing degrade-not-crash convention). torch/transformers excluded from base by design (PKG-03).

    Delete web-app artifacts (D-05/D-06) via git rm -r:
    app/ (contains the exec() module fetcher + unsafe_allow_html — must NOT ship), deployment/, .streamlit/, apt.txt, packages.txt.
    Also git rm requirements.txt — superseded by pyproject.toml; keeping both re-creates the duplicated-manifests drift documented in CONCERNS.md:42-45 (planner discretion; recoverable from git).

    Edit .planning/PROJECT.md (D-09):
    - Line 60 "**Tech stack**: Python 3.8+ (runtime) / 3.10 (dev), pip packaging." → "**Tech stack**: Python 3.11+ (runtime and dev), pip packaging (hatchling src-layout)."
    - Active item "Installable via pip (`pip install chat-analyzer-pro` + `analyze` command)" → replace `analyze` with `chat-analyzer` (aligns the doc with locked decision D-01).
    No other PROJECT.md content changes.
  </action>
  <verify>
    <automated>
      # 1. pyproject structural assertions (tomllib is stdlib 3.11+)
      python -c "
import tomllib, pathlib
d = tomllib.loads(pathlib.Path('pyproject.toml').read_text())
assert d['project']['requires-python'] == '>=3.11', d['project']['requires-python']
assert d['project']['scripts']['chat-analyzer'] == 'chat_analyzer.cli:app'
assert d['project']['optional-dependencies']['nlp'] == ['torch>=2.0', 'transformers>=4.30,<6']
base = ' '.join(d['project']['dependencies']).lower()
for forbidden in ('torch', 'transformers', 'streamlit', 'plotly', 'nltk'):
    assert forbidden not in base, forbidden
assert 'typer' in base and 'rich' in base and 'plotext' in base
print('PYPROJECT-OK')
"

      # 2. Deletions complete
      @('app','deployment','.streamlit','apt.txt','packages.txt','requirements.txt') | ForEach-Object { Test-Path $_ }

      # 3. PROJECT.md floor fixed
      Select-String -Path .planning/PROJECT.md -Pattern "3\.8" | Measure-Object | Select-Object -ExpandProperty Count
    </automated>
  </verify>
  <acceptance_criteria>
    - python -c tomllib check prints PYPROJECT-OK (requires-python == ">=3.11"; scripts.chat-analyzer == "chat_analyzer.cli:app"; nlp extra exactly ["torch>=2.0", "transformers>=4.30,<6"]; base deps contain typer/rich/plotext and contain NONE of torch/transformers/streamlit/plotly/nltk)
    - All six Test-Path checks return False (app/, deployment/, .streamlit/, apt.txt, packages.txt, requirements.txt deleted)
    - Zero matches for "3.8" in .planning/PROJECT.md
    - git status shows the deletions staged (or committed per repo flow)
  </acceptance_criteria>
  <done>
    pyproject.toml is valid and structurally enforces PKG-02/03/04; web-app code and stale manifests are gone from the repo; PROJECT.md no longer advertises the obsolete 3.8 floor and names the correct command.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Package legitimacy gate — verify new install-time dependencies before any pip install (blocking)</name>
  <what-built>
    Nothing built — this is the pre-install gate required by the package legitimacy protocol. Plan 01 created pyproject.toml whose base dependencies add four packages NOT previously in the repo's requirements.txt: typer, rich, plotext (CLI stack) and hatchling (build backend). All other base deps were already declared in requirements.txt. The research stack files (STACK.md) could not include a formal `## Package Legitimacy Audit` table, so per the fallback policy these four are treated as [ASSUMED] and require human verification before the pip install that plan 02 performs.
  </what-built>
  <how-to-verify>
    1. Open each package page in a browser and confirm it exists, is the documented real package (not a typosquat), and the version available matches the range used in pyproject.toml:
       - https://pypi.org/project/typer/ — expect typer 0.27.x (verified click-free, rich>=13.8 hard dep per research)
       - https://pypi.org/project/rich/ — expect rich 15.x
       - https://pypi.org/project/plotext/ — expect plotext 5.3.x (zero-dependency ASCII charts)
       - https://pypi.org/project/hatchling/ — expect hatchling 1.31.x (build backend)
    2. Sanity-check each: correct project name matches what pyproject declares, maintainer looks legitimate, no suspicious recent takeover (metadata visible on the PyPI page).
    3. If all four check out, approve. If any looks wrong ([SUS]/[SLOP]), report which one and what you saw — do NOT approve.
  </how-to-verify>
  <resume-signal>Type "approved" to let plan 02 proceed with `pip install -e .`, or describe the issue found for any package.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| build config → PyPI | Dependency resolution pulls third-party packages (typer, rich, plotext, hatchling) into the install |
| package restructure → import machinery | Renamed package markers change what code executes at import time; stale `src.*` imports would raise at runtime |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-SC | Tampering | pip installs of new deps (typer, rich, plotext, hatchling) | mitigate | Blocking-human checkpoint (Task 3) verifies each on pypi.org/project/* before any install; research STACK.md already verified versions against PyPI JSON API; [ASSUMED] fallback documented — checkpoint is never auto-approvable |
| T-01-01 | Tampering | src/chat_analyzer/** (moved modules) | mitigate | Stale `from src.*` imports would silently break wheel imports — grep gate asserts 0 matches (Select-String over all moved .py files) + compileall + import smoke in Task 1 verify |
| T-01-02 | Tampering | src/chat_analyzer/__init__.py | mitigate | Eager `from . import ...` lines removed — importing chat_analyzer must not pull pandas/matplotlib/reportlab (would violate instant --help and mask missing-dep failures); sys.modules assertion in Task 1 verify |
| T-01-03 | Spoofing | repo content (supply chain via local clone) | accept | Local CLI, D-07 clone-and-run distribution; no remote code executed anywhere in the package (the app/ exec() fetcher is deleted in Task 2); user installs from their own clone |
</threat_model>

<verification>
- `python -m compileall -q src/chat_analyzer` — exit 0
- `Get-ChildItem src/chat_analyzer -Recurse -Filter *_init_.py` — 0 files
- `Get-ChildItem src/chat_analyzer -Recurse -Filter *.py | Select-String -Pattern "from src\.|import src\."` — 0 matches
- PYTHONPATH=src import smoke prints IMPORTS-OK; light-package check prints LIGHT-OK
- tomllib structural assertions print PYPROJECT-OK
- Deletion Test-Path checks all False; PROJECT.md has no "3.8"
- Task 3 blocking-human gate approved
</verification>

<success_criteria>
- [ ] src/chat_analyzer/ is a valid import package (markers, imports, lazy summarizer) verified by automated checks
- [ ] pyproject.toml enforces requires-python >=3.11, lean base deps, [nlp] extra, and D-01 console script target
- [ ] app/, deployment/, .streamlit/, apt.txt, packages.txt, requirements.txt deleted; PROJECT.md floor fixed
- [ ] Package legitimacy gate human-approved (typer, rich, plotext, hatchling)
- [ ] Plan 02 can now `pip install -e .` and verify the console script + python -m entry points
</success_criteria>

<output>
Create `.planning/phases/01-package-foundation/01-01-SUMMARY.md` when done
</output>
