# Heroku Procfile for Chat Analyzer Pro
# Specifies commands to run on startup

# Web process - Run Streamlit app
web: streamlit run app/streamlit_app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true

# Alternative: Run with Gunicorn (if using Flask backend)
# web: gunicorn app:app --bind 0.0.0.0:$PORT --workers 4

# Worker process (optional) - For background tasks
# worker: python src/utils/background_worker.py

# Release phase (optional) - Run before deployment
# release: python scripts/migrate.py
